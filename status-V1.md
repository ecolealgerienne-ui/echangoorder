# Status V1 — Echango Order (MVP Phase 1)

Suivi de l'avancement de l'implémentation. Statuts : `Non démarré` · `En cours` · `Bloqué` · `Terminé`.

Dernière mise à jour : 2026-07-19

**Stratégie en cours** : phase 1 = écrans + navigation complets, sans données ni backend (état local simulé uniquement pour piloter la navigation : session auth). Phase 2 = branchement direct sur Odoo (pas de couche mock intermédiaire).

**⚠️ Changement de stack (2026-07-18)** : le projet a démarré en React Native puis a été **entièrement basculé sur Flutter** en cours de route (choix de l'équipe, expérience Flutter préalable). Tout le code React Native a été supprimé et réécrit en Flutter/Dart — même périmètre fonctionnel (F00-F17), mêmes clés i18n, même logique de navigation, portés dans un nouveau framework. Voir CLAUDE.md § Stack technique pour la note de décision.

**⚠️ Changement PIN (2026-07-18)** : les specs prévoient un PIN à 4 chiffres partout ; décision produit de passer à **6-12 chiffres** (plus d'entropie). Voir CLAUDE.md § Stack technique.

---

## 0. Setup projet

| Tâche | Statut | Notes |
|---|---|---|
| Repo Git créé (`main` + branche de travail) | Terminé | Repo initialisé, docs ajoutés |
| Specs macro & Phase 1 versées dans `docs/` | Terminé | `docs/specs_macro_drive_transport.md`, `docs/specs_phase1_echango_order.md` |
| `CLAUDE.md` rédigé | Terminé | |
| Scaffolding app Flutter | Terminé | `mobile/`, `pubspec.yaml` + `lib/` écrits par Claude Code. `android`/`ios` générés côté utilisateur via `flutter create --org com.echangoorder .` |
| Navigation (go_router : routes publiques + StatefulShellRoute tabs) | Terminé — `flutter analyze` OK | Tous les écrans F00-F17 navigables de bout en bout (`mobile/lib/navigation/app_router.dart`). `flutter analyze` passé côté utilisateur (2 warnings cosmétiques corrigés : import inutile, doc comment) |
| Config i18n FR/AR + RTL | Terminé (base) | `easy_localization`, fichiers `assets/translations/fr.json`/`ar.json`. RTL géré nativement par Flutter (`Directionality` auto sur `Locale('ar')`) — pas de redémarrage app nécessaire contrairement à React Native. Reste à valider visuellement sur device/émulateur |
| État local (session, onboarding) pour piloter la navigation | Terminé | `state/auth_state.dart` (`ChangeNotifier` + `provider`), branché sur le `redirect` de `go_router`, **persisté via `shared_preferences`** (survit au redémarrage de l'app) — sera remplacé par la vraie session Odoo (F02) |
| F14 — Permissions natives réelles (GPS, notifications) | Terminé (code) | `permission_handler` : dialog d'explication + demande système, gestion du refus sans bloquer l'app. **Déclarations natives Android (`AndroidManifest.xml`) et iOS (`Info.plist`/`Podfile`) pas encore ajoutées** — voir CLAUDE.md, à faire après génération de `android/`/`ios/` |
| F17 — Écran substitution produit | Terminé | Accessible via bouton démo depuis le suivi de commande (pas de vrai trigger tant que F11 n'existe pas) |
| F12 — Bouton partage produit | Terminé (partiel) | `share_plus`, sheet natif avec lien placeholder. Réception du deep link non câblée (nécessite domaine + choix de techno, cf. §4) |
| F10 — Suppression de compte (popup + PIN) | Terminé (UI) | `delete_account_dialog.dart` — saisie 6-12 chiffres via `PinInputField`, aucune validation réelle du PIN contre un compte (attend Odoo) |
| Nouvelles dépendances (`permission_handler`, `share_plus`, `shared_preferences`) | Terminé — `flutter analyze` OK | Corrigé suite au 1er `flutter analyze` local : `SharePlus.instance.share(ShareParams(...))` n'existait pas dans la version résolue → remplacé par `Share.share(text)` (API historique, stable). `test/widget_test.dart` généré par `flutter create .` référençait l'ancien template (`MyApp`) → réécrit pour pointer sur `EchangoOrderApp` avec `SharedPreferences.setMockInitialValues({})` |
| Gestion d'erreurs centralisée (codes + i18n) | Terminé (infra) | `lib/errors/` : `AppError` (codes en dot-path mappés 1:1 sur `errors.*` dans les traductions), `AppMessenger` (seul point d'affichage : snackbar erreur/info, dialog bloquant), `ErrorStateView` (état plein écran réutilisable, vides + erreurs). Tous les `ScaffoldMessenger`/messages en dur existants migrés (`coming_soon.dart`, `permission_service.dart`, `MaintenanceScreen`). Convention documentée dans CLAUDE.md — **obligatoire pour tout nouveau code**, y compris le futur client Odoo (mapper les erreurs JSON-RPC vers ces mêmes codes) |
| Validation de formulaire centralisée | Terminé (infra) | `lib/validation/validators.dart` : `validatePhone`, `validatePin` (6-12 chiffres), `validateRequired`, `validatePinMatch` — retournent des `AppError?`, affichées via `AppMessenger` |
| Champ PIN réutilisable | Terminé | `widgets/pin_input_field.dart` — masqué, bascule visibilité, 6-12 chiffres. Câblé (vrais champs + validation) dans Login, RegisterStep1 (téléphone) et RegisterStep3, ChangePin, ForgotPin, DeleteAccount. Aucune vérification contre un vrai compte (attend Odoo) |
| États vides (Cart, OrderHistory, Search) | Terminé | `ErrorStateView` appliqué aux 3 écrans (specs QA : panier vide, aucune commande — avec message spécifique invité, aucun résultat recherche). Boutons démo conservés pour continuer à tester la navigation checkout/suivi/fiche produit tant qu'il n'y a pas de vraies données |
| Numéro de version dans "À propos" | Terminé | `package_info_plus`, F14 QA "Numéro de version visible et à jour" |
| Vérification automatisée des traductions FR/AR | Terminé | `test/translations_completeness_test.dart` — audit demandé suite à un signalement "traductions AR manquantes" (titre "Mon Panier", plusieurs boutons). Audit complet effectué (clés `.tr()` statiques + tous les `screenKey` × title/subtitle) : **aucune clé manquante trouvée** dans le code actuel, fr.json et ar.json parfaitement synchronisés. Seul écart réel trouvé et corrigé : 2 labels "Réf :" en dur (→ `common.reference`). Le signalement initial est probablement un souci de rebuild (assets JSON non rechargés en hot reload) plutôt qu'une vraie traduction manquante — ce test tourne maintenant en continu pour objectiver ça à l'avenir |
| Environnement Odoo 19 + Postgres (Docker/WSL) | Terminé — exécuté et validé côté utilisateur | `backend/docker-compose.yml` (odoo:19 + postgres:16), `backend/config/odoo.conf`, module `backend/addons/echango_order/`. `docker compose up` réel effectué en WSL, base créée, module installé/mis à niveau avec succès. Voir CLAUDE.md § Environnement de dev — backend Odoo |
| Module Odoo F02 — auth téléphone/PIN | Terminé (code) — inscription et connexion validées en réel | `backend/addons/echango_order/` : `models/res_users.py` (champs `x_pin` haché, `x_pin_fail_count`, `x_pin_locked_until` sur `res.users` — voir CLAUDE.md § Principe architecture Odoo), `controllers/auth_controller.py` (`/echango/auth/register`, `/echango/auth/login`). Identité client = utilisateur portail Odoo (`base.group_portal`) lié à un `res.partner`, pas de modèle custom séparé. Connexion branchée sur le point d'extension standard `res.users._check_credentials` (type `pin`, mécanisme utilisé par `auth_oauth`/`auth_ldap`). **Testé en réel via curl contre un Odoo 19 réellement lancé (WSL)** : `/register` puis `/login` retournent `{"success": true, ...}` — 3 aller-retours nécessaires pour corriger des changements internes Odoo 19 découverts en testant (signature de `session.authenticate(env, credential)`, `res.partner.mobile` renommé `phone`, `res.users.groups_id` renommé `group_ids`), tous corrigés et confirmés contre le code source officiel d'Odoo 19. Délai anti brute-force implémenté en fenêtre `locked_until` (pas de `time.sleep`) : 1/2/4/8s puis **15 min de blocage au 5e échec — durée arbitraire, à valider avec le PO** — mécanisme codé mais le scénario de verrouillage (5 échecs consécutifs) n'a pas encore été testé en réel. **Bug corrigé (remonté via F06)** : `register()` écrivait `lang: "fr_FR"` sans vérifier que le français est réellement installé dans Odoo — un compte créé avec une langue non installée faisait planter la création de `sale.order` plus tard ("Invalid language code", mécanisme de notification/mail interne à Odoo qui valide la langue du partenaire). Corrigé : la langue demandée n'est assignée que si elle apparaît dans `res.lang.get_installed()`, sinon `lang=False` (repli automatique standard d'Odoo). **Les comptes de test déjà créés avant ce correctif gardent `lang="fr_FR"` invalide en base** — soit réinscrire un compte de test, soit corriger la langue du contact concerné dans Réglages → Utilisateurs & Sociétés → Contacts |
| Client API JSON-RPC Odoo (Flutter) | Terminé (F02) — **validé en réel par l'utilisateur** (inscription + connexion via l'app, sans erreur) | `lib/services/odoo_api_client.dart` (`register`/`login`, appelle `/echango/auth/*`, cookie de session Odoo gardé en mémoire) + `lib/config/api_config.dart` (URL de base dev, `10.0.2.2` sur émulateur Android). Branché sur `LoginScreen` et `RegisterStep3Screen` (le téléphone saisi à l'étape 1 transite via `extra` de go_router jusqu'à l'étape 3 pour l'appel `register`). Toutes les constantes `AppError` nécessaires existaient déjà (`authInvalidCredentials`, `authPhoneAlreadyUsed`, `authPinLocked`, `validationRequired`, `validationInvalidPin`) — mappage fait dans `odoo_api_client.dart`, aucune traduction à ajouter. Logs backend confirment aussi le délai anti brute-force (tentatives PIN incorrectes court-circuitées par la fenêtre `locked_until` sans même atteindre `res.users.authenticate`). **Limite connue** : cookie de session gardé en mémoire seulement (pas persisté) — perdu au redémarrage de l'app ; le stockage sécurisé (`flutter_secure_storage`, voir ligne juste en dessous) reste à faire avant une vraie gestion de session 24h. `_rpc()` généralisé pour supporter aussi le `/web/dataset/call_kw` standard (voir F03 ci-dessous), pas seulement les 2 endpoints custom d'auth |
| F03 — Accueil : affichage produits (Flutter + backend) | **Terminé — validé en réel** | **Backend** : `security/ir.model.access.csv` + `security/ir_rule.xml` dans `echango_order` — le groupe portail n'avait par défaut AUCUN accès à `product.template`/`product.category` (vérifié contre le code source Odoo 19 des modules `product` et `sale`, aucune entrée portail trouvée), donc accès lecture seule accordé + `ir.rule` restreignant aux produits `sale_ok = true` (pas de fuite du catalogue interne). **Flutter** : `HomeScreen` réécrit — `OdooApiClient.searchRead()` (`search_read` standard via `call_kw`, pas de contrôleur custom) sur `product.template` (`name`, `list_price`, `image_128`), grille 2 colonnes, état vide et état d'erreur via `ErrorStateView` existant. **Écart assumé vs spec Expert Odoo** : filtre `sale_ok = true` à la place de `is_published` (champ qui n'existe pas sans le module `website_sale`, non installé) — pas de notion de "produits mis en avant" pour l'instant, tous les produits vendables s'affichent ; la curation viendra avec `x_vitrine_publique` (F00) si besoin. Piège rencontré en testant : `docker compose exec odoo odoo -u ...` contourne l'entrypoint de l'image officielle (qui traduit `HOST`/`USER`/`PASSWORD` en paramètres de connexion) — nécessite `--db_host=db --db_user=odoo --db_password=odoo` explicites pour cette commande ponctuelle (le `docker compose restart odoo` normal repasse bien par l'entrypoint, lui) |
| F04 — Catalogue & Recherche (Flutter + backend) | Terminé (code), non re-testé après correction | **`qty_available` retiré (3e itération)** : accordé l'accès `product.product` (voir historique précédent) pensant que ça suffirait, mais le calcul de `qty_available` va ENSUITE lire `stock.warehouse` (et probablement encore plus loin — quants, emplacements) : nouvelle `AccessError` remontée par l'utilisateur. Plutôt que de continuer à ouvrir l'accès module de stock par module (surexposition de la mécanique interne d'inventaire à un compte client, contraire à CLAUDE.md § Sécurité), **`qty_available` est retiré des champs demandés** en Catalogue/Recherche — le badge "Épuisé" du `ProductGridTile` reste dans le code mais restera inactif (`qty` toujours `null`) tant qu'on n'aura pas un vrai signal de disponibilité, plus étroit, à concevoir séparément (probablement au moment de F07/checkout `checkout.out_of_stock`, pas dans cette passe). L'accès `product.product` + son `ir.rule` ajoutés pour la 2e itération sont **retirés** de `security/` (redevenus inutiles) — un nouveau `-u echango_order` les supprimera de la base (Odoo nettoie les enregistrements dont l'external ID a disparu du module). **Flutter** : `CategoryProductsScreen` (grille filtrée `categ_id`, nom de catégorie transmis via `extra` de go_router depuis `CatalogScreen`), `SearchScreen` (`name ilike`, anti-rebond 400ms via `Timer`, pas de nouvelle dépendance). Nouveau widget partagé `lib/widgets/product_grid_tile.dart` (`ProductGridTile`) utilisé par Accueil/Catalogue/Recherche — évite de dupliquer 3 fois le décodage d'image + la mise en page ; badge "Épuisé" (`qty_available <= 0`) et bouton "+" optionnel (absent sur l'Accueil, présent en Catalogue/Recherche comme dans le wireframe) qui appelle `showComingSoon` (ajout réel au panier = F06, pas fait). Nouvelles clés `catalog.outOfStock` + `emptyStates.categories{Title,Message}` en FR/AR. **Bug signalé par l'utilisateur (capture d'écran) et corrigé** : `CatalogScreen` faisait un `search_read` direct sur `product.category`, ce qui remontait aussi les catégories techniques par défaut d'Odoo (Dépenses, Achats, Construction...) sans aucun produit vendable dedans. Remplacé par un `read_group` sur `product.template` (`groupby: categ_id`) — nouvelle méthode `OdooApiClient.readGroup()` — qui ne fait remonter que les catégories ayant réellement des produits visibles côté portail, avec le nombre de produits par catégorie en bonus. `security/ir.model.access.csv` gardé tel quel (accès `product.category` toujours nécessaire : Odoo doit lire ce modèle en interne pour résoudre le nom affiché du many2one `categ_id`, même sans `search_read` direct dessus depuis Flutter). **Non fait dans cette passe** : barre de recherche accessible depuis l'Accueil (QA F03, jamais implémentée, risque de complications de navigation cross-onglet avec `StatefulShellRoute` — à traiter séparément si besoin) |
| F04 — nettoyage du warning `read_group` déprécié | Terminé | `read_group` (utilisé pour les catégories du catalogue) affichait `DeprecationWarning: Since 19.0, read_group is deprecated` dans les logs Odoo à chaque chargement de l'écran Catalogue — pas une erreur, mais un signal que la méthode sera retirée dans une future version. Migré vers `formatted_read_group` (vérifié contre le code source, `addons/web/models/models.py`) : même représentation `[id, nom]` pour `categ_id`, mais le comptage n'est plus implicite — demandé explicitement via `aggregates: ['__count']` (valeur par défaut de `OdooApiClient.readGroup()`), clé de résultat `__count` au lieu de `categ_id_count` |
| F05 — Fiche Produit (Flutter + backend) | Terminé (code), non testé en réel | **Backend** : accès portail ajouté pour `uom.uom` (`security/ir.model.access.csv`) — vérifié au préalable contre le code source Odoo 19 (`addons/uom/security/ir.model.access.csv`) qu'aucune entrée portail n'existait déjà, comme pour `product.category`/`product.template` en F03 ; nécessaire pour résoudre le nom affiché du many2one `uom_id`. Pas de nouvel `ir.rule` (unités de mesure non sensibles, pas de filtrage nécessaire). Nécessite `-u echango_order`. **Flutter** : `ProductDetailScreen` réécrit — `OdooApiClient.read()` (nouvelle méthode, `read` standard par id, plus direct qu'un `search_read` avec domaine `id=`), champs `name`/`description`/`list_price`/`image_1920`/`uom_id`. `description` est un champ **Html** côté Odoo 19 (vérifié contre le code source, pas un `Text`/`Char` comme les specs le suggéraient implicitement) — balises retirées côté client par une regex plutôt que d'ajouter une dépendance de rendu HTML pour ça. Sélecteur de quantité local (min 1), bouton "Ajouter au panier" branché sur F06 (voir ligne ci-dessous). Bouton de partage F12 existant conservé tel quel. **`qty_available` volontairement absent** (même décision qu'en F04) : le bouton "Ajouter au panier" reste toujours actif, critère QA "produit épuisé → bouton désactivé" différé — voir point de vigilance §4 |
| F06 — Panier (Flutter + backend) | Terminé (code), non testé en réel | **Backend** : `controllers/cart_controller.py` (nouveau) — `/echango/cart`, `/echango/cart/add`, `/echango/cart/update`, `/echango/cart/remove`. Vérifié au préalable contre le code source Odoo 19 (`addons/sale/security/ir.model.access.csv` + `ir_rules.xml`) : le groupe portail a déjà un accès **lecture seule** à `sale.order`/`sale.order.line` (`1,0,0,0` — ni write, ni create, ni unlink), une limite de sécurité **volontaire** d'Odoo (pas un oubli de config comme pour le catalogue), pour empêcher un client de manipuler directement des documents de vente via l'ORM. D'où un contrôleur custom (comme F02) plutôt qu'un élargissement des droits ORM bruts : il retrouve/crée le devis brouillon du partenaire connecté, vérifie la propriété de chaque ligne avant modification/suppression, et agit en `sudo()`. Le panier EST le devis Odoo (`sale.order` état `draft`) — pas un état local à resynchroniser plus tard, conforme à CLAUDE.md § Principe architecture Odoo. Aucun nouveau fichier `ir.model.access`/`ir.rule` nécessaire (le contrôleur fait tout en `sudo()`, Flutter n'appelle jamais `sale.order`/`sale.order.line` en direct). **Flutter** : `state/cart_state.dart` (`CartState`, `ChangeNotifier` comme `AuthState`), 4 nouvelles méthodes sur `OdooApiClient`, `CartScreen` réécrit (liste, +/-, suppression, sous-total/total), badge sur l'onglet Panier (`main_tab_scaffold.dart`, widget Material `Badge` standard). Ajout au panier branché sur `ProductDetailScreen` (avec la quantité choisie) et sur le bouton "+" du `ProductGridTile` (Catalogue/Recherche). Nouveau `utils/require_account.dart` : un invité qui tente d'ajouter au panier se voit proposer l'inscription (`actions.signUpToOrder`, traduction déjà prête mais jamais branchée jusqu'ici) plutôt que d'appeler un endpoint `auth='user'` qui échouerait. **Panier invité non implémenté** (commande liée à un partner temporaire Odoo, cf. specs §"Mode invité" — décision produit déjà notée comme ouverte en §4, pas traitée dans cette passe) : un invité voit l'état "panier vide" existant, sans appel API. Nouvelle erreur `cart.product_unavailable` mappée vers la constante `AppError.checkoutOutOfStock` déjà existante (même message, pas de nouvelle traduction) ; erreurs "ligne introuvable" mappées vers `AppError.notFound` (déjà existante) |
| Persistance de session (Flutter) | Terminé (code), non testé en réel | `flutter_secure_storage` ajouté (Keychain/Keystore, cf. CLAUDE.md § Sécurité). `OdooApiClient` : `restoreSession()` (lu au démarrage dans `main()`, avant `runApp`, avec `AuthState`/`CheckoutState` désormais créés dans `main()` plutôt que dans le `State` de l'app pour permettre ce `await` préalable), persistance du cookie à chaque réponse (`_rpc()`), `clearSession()` (appelé au logout, `ProfileScreen`). Pas de vérification proactive de validité au démarrage (pas d'appel "ping") : callback `onSessionExpired` déclenché par `_rpc()` quand le mapping d'erreur donne `AppError.authSessionExpired`, qui appelle `AuthState.logout()` — le `redirect` de go_router (déjà branché sur `AuthState`) renvoie alors automatiquement vers les routes publiques. `test/widget_test.dart` mis à jour (nouvelle signature `EchangoOrderApp`) |
| Signal de disponibilité stock (Flutter + backend) | Terminé (code), non testé en réel | **Backend** : `controllers/catalog_controller.py` (nouveau) — `/echango/catalog/stock`, calcule `qty_available` en `sudo()` sur une liste d'ids (`product.template`, filtré `sale_ok`) sans exposer `product.product`/`stock.warehouse` au portail. `cart_controller.py.add()` valide aussi le stock côté serveur (pas seulement l'UI) avant d'ajouter une ligne, réutilise `cart.product_unavailable`. **Flutter** : `OdooApiClient.getStock()`, appelé en 2e temps après le chargement des produits dans `CategoryProductsScreen`/`SearchScreen`/`ProductDetailScreen` (fusionné dans les maps produit sous la clé `qty_available` déjà attendue par `ProductGridTile`, donc le badge "Épuisé" s'active sans changement côté widget). `AppButton.onPressed` rendu nullable (`VoidCallback?`) pour pouvoir désactiver "Ajouter au panier" + message `errors.checkout.out_of_stock` sur la fiche produit quand épuisé — critère QA F05 enfin couvert |
| F07 — Checkout & Mode de Réception (Flutter + backend) | Terminé (code), non testé en réel | **Backend** : `models/sale_order.py` (`x_reception_mode` Selection, `x_creneau` Datetime — début du créneau, durée fixe 2h par convention, pas de champ de fin séparé) ; `models/delivery_zone.py` (nouveau modèle `x_delivery_zone` : name/city/zip_code, géré par un menu back-office propre "Echango Order" ajouté par `views/delivery_zone_views.xml`, accès réservé aux utilisateurs internes — **pas exposé au portail**, la vérification de zone passe par un contrôleur en `sudo()`). `controllers/checkout_controller.py` (nouveau) : `/echango/checkout/check_zone` (ville/code postal vs `x_delivery_zone`, matching simple `=ilike`/`=` — pas de normalisation avancée) et `/echango/checkout/confirm` (fixe le mode/l'adresse (nouveau contact `type=delivery`)/le créneau puis `action_confirm()` — méthode standard vérifiée contre le code source `addons/sale/models/sale_order.py`, pas une écriture brute de `state`). **Flutter** : `state/checkout_state.dart` (`CheckoutState`, éphémère, non persisté, remis à zéro à chaque entrée dans le tunnel depuis `CartScreen` et après confirmation), `utils/timeslots.dart` (créneaux fixes de 2h générés côté client — 10h/14h/16h aujourd'hui, 9h/11h demain — **pas de notion de créneau complet**, aucun modèle de capacité en back-office pour l'instant). Les 4 écrans réécrits : `CheckoutReceptionModeScreen` (choix réel), `CheckoutAddressScreen` (formulaire + vérification de zone + bouton GPS qui redemande la permission F14 mais **n'autoremplit pas** l'adresse — géocodage inverse nécessiterait un service externe, Google/Mapbox/Nominatim, décision non prise, `showComingSoon` pour cette seule partie), `CheckoutOutOfZoneScreen` (bascule vers retrait), `CheckoutTimeslotScreen` (sélection), `CheckoutSummaryScreen` (récapitulatif + appel réel de confirmation, panier rafraîchi ensuite — redevient vide puisque le devis n'est plus à l'état brouillon). Nouvelles clés `checkout.{streetLabel,cityLabel,zipLabel,notesLabel,today,tomorrow,paymentOnDelivery,itemsLabel}` en FR/AR |
| F08 — Confirmation (écran, Flutter) | Terminé (partiel, code) | `OrderConfirmationScreen` affiche maintenant les vraies données renvoyées par `/echango/checkout/confirm` (référence, montant, mode, créneau) transmises via `extra` de go_router, plus le `orderRef` en path param. **Suivi de commande en temps réel (statuts synchronisés + notifications push) hors scope de cette passe** — `order_tracking_screen.dart` affiche maintenant les vraies données de la commande (F09) mais pas d'étapes préparation/livraison synchronisées, ça reste un simple état Odoo (`state`) |
| F09 — Historique & Reorder (Flutter + backend) | Terminé (code), non testé en réel | **Backend** : `cart_controller.py.reorder()` (nouveau) — recopie les lignes d'une commande passée dans le panier en cours, exclut les produits non vendables/en rupture (même logique que `add()`), retourne la liste des noms exclus. Aucun nouvel accès nécessaire : `sale.order`/`sale.order.line` déjà lisibles par le portail pour ses propres commandes (règle standard `sale_order_rule_portal`/`sale_order_line_rule_portal`, vue en F06) — juste `search_read` classique pour la liste et le détail, pas de contrôleur custom pour la lecture. **Flutter** : `OdooApiClient.searchRead()` gagne un paramètre `order` (tri), `CartState.reorder()` (applique le panier renvoyé + expose la liste des indisponibles). `OrderHistoryScreen` et `OrderTrackingScreen` réécrits avec les vraies données. Popup de confirmation + avertissement si produits exclus + redirection vers le panier, conforme aux critères QA |
| F10 — Profil : en-tête réel + modification nom + PIN (Flutter + backend) | Terminé (code), non testé en réel | **Backend** : `controllers/profile_controller.py` (nouveau) — `/echango/profile` (lecture `name`/`phone`/`lang`/`partner_latitude`/`partner_longitude`), `/echango/profile/update_name`, `/echango/profile/change_pin`. Vérifié au préalable contre le code source Odoo 19 (`addons/base/security/ir.model.access.csv`) : `res.partner` est **lecture seule** pour le portail (`1,0,0,0`), même limite volontaire que `sale.order`/`sale.order.line` (F06) — d'où un contrôleur `sudo()` dédié plutôt qu'un élargissement des droits ORM. Aucun nouveau modèle/champ/fichier `security` (le contrôleur ne touche que des champs standards déjà en base) : **pas de `-u echango_order` nécessaire pour ce stage, un `docker compose restart odoo` suffit**. `change_pin` réutilise directement `res.users._check_pin`/`_set_pin` (F02) plutôt que de dupliquer la vérification — effet secondaire assumé : un mauvais PIN actuel pendant un changement compte aussi dans le compteur anti brute-force existant. **Bonus documentaire** : en explorant `res.partner` pour ce stage, confirmation que `partner_latitude`/`partner_longitude` sont des champs standards du module `base` lui-même (pas besoin de `base_geolocalize`) — `CLAUDE.md` mis à jour, `x_latitude`/`x_longitude` retirés de la liste des champs custom (même traitement que `x_langue` précédemment). **Flutter** : `OdooApiClient.getProfile()`/`updateProfileName()`/`changePin()`, `ProfileScreen` réécrit (en-tête nom/téléphone réel via `FutureBuilder`, tap sur le nom → `AlertDialog` d'édition), `ChangePinScreen` réécrit (branché sur le vrai endpoint, remplace le `showComingSoon`). **Téléphone volontairement en lecture seule** : c'est aussi l'identifiant de connexion (F02), le modifier nécessiterait une re-vérification par SMS non implémentée. Nouvelles clés `profile.{pinChanged,editNameTitle,nameUpdated}` en FR/AR. **Reste pour F10** : adresses (CRUD `res.partner` enfants type `delivery`), Ma Localisation (GPS réel + `partner_latitude`/`longitude`), suppression de compte réelle (actuellement UI seule, voir ligne plus haut), notifications (petit écran d'état des permissions) |
| F10 — Adresses sauvegardées (Flutter + backend) | Terminé (code), non testé en réel | **Backend** : nouveau modèle `models/res_partner.py` (`_inherit = "res.partner"`, champ `x_adresse_favorite` Boolean) — implémente exactement le champ déjà pré-approuvé dans CLAUDE.md § Custom fields Odoo attendus (Odoo n'a pas de notion de "default address" parmi les contacts enfants d'un partenaire). **Nécessite `-u echango_order`** (nouveau champ = nouvelle colonne DB), contrairement au stage précédent. `controllers/profile_controller.py` complété avec `list_addresses`/`add_address`/`update_address`/`remove_address` — adresses = contacts enfants standards de `res.partner` (`parent_id` + `type='delivery'`, même mécanisme que celui déjà utilisé côté checkout F07), toujours en `sudo()` avec vérification explicite de propriété (`parent_id` = partenaire courant) puisque `res.partner` reste en lecture seule pour le portail. Une seule adresse favorite à la fois : `_unset_other_favorites()` désactive les autres favoris du même parent avant d'en activer un nouveau. **Flutter** : `OdooApiClient.listAddresses/addAddress/updateAddress/removeAddress`, `AddressesScreen` réécrit (liste + carte par adresse avec icône étoile si favorite + suppression avec confirmation) et nouveau `_AddressFormScreen` (ajout/modif, champs nom/rue/ville/code postal/notes + case à cocher favorite). Nouvelles clés `common.delete` + section `addresses.*` en FR/AR. **Non fait dans cette passe** : le checkout (F07) ne propose pas encore de choisir une adresse sauvegardée — il continue de créer un contact `delivery` ad hoc à chaque confirmation (`checkout_controller.py` inchangé) ; réutiliser une adresse sauvegardée (et sa favorite par défaut) au checkout reste un point de vigilance à traiter séparément |
| Firebase Cloud Messaging configuré | Non démarré | |
| Stockage sécurisé PIN (Keychain/Keystore) | Non démarré | Côté Flutter : `flutter_secure_storage` maintenant ajouté (utilisé pour la session, voir ligne ci-dessus) mais **pas encore appliqué au PIN lui-même** côté device (le PIN n'est de toute façon jamais stocké côté app, seul le hash vit côté Odoo — cette ligne concerne un éventuel besoin futur de mémoriser un PIN localement, à clarifier) |
| Build & run réel sur simulateur/device (iOS/Android) | En cours | `flutter analyze` validé côté utilisateur (une passe, ancienne) ; volume important de nouveau code depuis (F02-F08 branchés sur Odoo) jamais repassé en `flutter analyze`/`flutter run` complet. Non testable dans l'environnement Claude Code (pas de SDK Flutter/Android/Xcode) |

## 1. Fonctionnalités Phase 1 (F00–F17)

Colonne **Écrans** : placeholders créés + navigables (sans données ni logique métier réelle). Colonne **API Odoo** : intégration backend réelle.

| # | Fonctionnalité | Écrans | API Odoo | QA / critères d'acceptation | Notes |
|---|---|---|---|---|---|
| F00 | Vitrine publique | Terminé | ☐ | ☐ | Nécessite champ `x_vitrine_publique` |
| F01 | Onboarding | Terminé | — | ☐ | Aucun appel API |
| F02 | Authentification (inscription/connexion/invité) | Terminé | Terminé — inscription + connexion validées en réel via l'app | ☐ | Backend Odoo `/echango/auth/register` + `/login` + client Flutter, testés avec succès de bout en bout (app réelle sur émulateur). Reste : scénario de blocage PIN (5 échecs) pas testé en réel, session non persistée (voir §0), et QA complet (RTL, perf) pas encore passé en revue formelle. Mode invité géré localement (pas d'appel Odoo) |
| F03 | Accueil | Terminé | **Terminé — validé en réel** (grille produits affichée dans l'app après `-u echango_order`) | ☐ | Voir §0 pour le détail (accès portail accordé, filtre `sale_ok`, pas encore de "produits mis en avant"/bannière) |
| F04 | Catalogue & Recherche | Terminé | Terminé (code) — corrigé après retours utilisateur (catégories + warning `read_group`), non re-testé depuis | ☐ | `CatalogScreen`/`CategoryProductsScreen`/`SearchScreen` (voir §0). Bouton "+" branché sur F06 (plus `showComingSoon`). Restriction `sale_ok` imposée côté serveur par l'`ir.rule` de F03 |
| F05 | Fiche Produit | Terminé | Terminé (code), non testé en réel | ☐ | Voir §0 — `read` standard par id, description Html nettoyée côté client, `qty_available` volontairement absent (bouton "Ajouter au panier" toujours actif, critère QA "épuisé → désactivé" différé) |
| F06 | Panier | Terminé | Terminé (code), non testé en réel | ☐ | Voir §0 — panier = devis Odoo (`sale.order` brouillon) réel, contrôleur custom (portail en lecture seule sur ces modèles par design Odoo). Pas de panier invité (partner temporaire non implémenté, cf. §4) |
| F07 | Checkout & Mode de Réception | Terminé | Terminé (code), non testé en réel | ☐ | Voir §0. GPS n'autoremplit pas l'adresse (décision service de géocodage non prise), pas de notion de "créneau complet" |
| F08 | Confirmation & Suivi Commande | Terminé | Confirmation : terminé (code). Suivi temps réel : non démarré | ☐ | Écran de confirmation avec données réelles (voir §0). Suivi (statuts synchronisés + notifs push) reste un placeholder — dépend de F11 |
| F09 | Historique Commandes & Reorder | Terminé | Terminé (code), non testé en réel | ☐ | Voir §0. Bouton reorder affiché sur toute commande confirmée (simplification — pas de distinction "Livrée" tant que F08/stock.picking n'est pas synchronisé) |
| F10 | Profil Utilisateur | Terminé | En cours — en-tête + nom + PIN + adresses terminés (code), non testé en réel | ☐ | Voir §0. Reste : Ma Localisation (GPS réel), suppression de compte réelle, notifications. Suppression compte : popup + saisie PIN (6-12 chiffres) implémentés (`delete_account_dialog.dart`) ; validation réelle du PIN + SMS de confirmation nécessitent Odoo |
| F11 | Notifications Push | Non démarré | ☐ | ☐ | Webhook Odoo → FCM. La permission notification (F14) est déjà demandée après confirmation de commande, mais aucun envoi réel |
| F12 | Partage Produit (Deep Link) | Terminé (partiel) | ☐ | ☐ | Bouton partage sur fiche produit (`share_plus`, sheet natif) avec lien placeholder. **Réception du deep link (Universal/App Links) non câblée** — nécessite un domaine réel + choix de techno (Branch.io vs Firebase Dynamic Links, cf. §4) + fichiers natifs `android/`/`ios/` |
| F13 | Pages Légales | Terminé (placeholder) | — | ☐ | Contenu réel à intégrer + validation juriste avant soumission stores |
| F14 | Permissions & États Système | Terminé | ☐ | ☐ | Écran maintenance + permissions réelles (`permission_handler`) : dialog d'explication puis demande système pour GPS (Register/CheckoutAddress/MyLocation) et notifications (auto après confirmation commande). **Déclarations natives Android/iOS pas encore ajoutées** (voir CLAUDE.md) |
| F15 | Code Promo | Terminé (placeholder dans récap checkout) | ☐ | ☐ | Module coupon Odoo 19 à valider |
| F16 | Annulation Commande | Terminé (popup confirmation) | ☐ | ☐ | Délai d'annulation à définir (PO) |
| F17 | Substitution Produit | Terminé | ☐ | ☐ | Écran substitution (produit original/suggestion, accepter/refuser). Accessible pour l'instant via un bouton démo dans le suivi de commande — en réel, déclenché par une notification push (dépend de F11) |

## 2. Sécurité (transversal — bloquant pour release)

| Exigence | Statut |
|---|---|
| HTTPS/TLS 1.3 sur tous les appels | Non démarré |
| PIN jamais stocké en clair (Keychain/Keystore) | Terminé | Le PIN n'est jamais persisté côté app (ni en clair ni haché) — seul le hash vit côté Odoo (`res.users.x_pin`, voir F02). Rien à stocker côté Keychain/Keystore pour le PIN lui-même |
| Session 24h + re-auth PIN | En cours | Session persistée (Keychain/Keystore, voir §0) et réaction automatique à l'expiration côté serveur (`onSessionExpired` → déconnexion locale). Reste à faire : politique d'expiration explicite à 24h côté Odoo (dépend de sa config par défaut, non vérifiée) et écran de ré-authentification par PIN spécifique (pour l'instant, expiration = retour pur et simple à l'écran de connexion) |
| Délai progressif tentatives PIN (1s/2s/4s/8s + blocage) | En cours — implémenté serveur (`res.users._check_pin`), non testé en réel |
| Filtrage strict des champs API (pas de surexposition) | En cours | Discipline appliquée à chaque fonctionnalité (accès portail minimal + `ir.rule`, contrôleurs `sudo()` dédiés pour tout ce qu'Odoo restreint volontairement — cart, checkout, stock) plutôt qu'élargir les droits ORM bruts. Pas encore un audit formel de bout en bout |
| Rate limiting endpoints publics (vitrine, deep links) | Non démarré |

## 3. i18n / RTL (transversal)

| Exigence | Statut |
|---|---|
| Externalisation complète des chaînes (fichiers de traduction FR/AR) | Terminé — vérifié automatiquement (`translations_completeness_test.dart`) |
| Layout RTL validé sur chaque écran | Non démarré |
| Formats date/heure localisés | Non démarré |

## 4. Points de vigilance ouverts (décisions à prendre — voir specs §6 / §7)

- [ ] Valider les statuts de commande Odoo avec l'Expert Odoo
- [ ] Définir le délai d'annulation commande autorisé
- [ ] Définir le délai de conservation des données après suppression de compte (RGPD)
- [ ] Définir les zones de livraison et codes postaux couverts
- [ ] Valider contenu CGU / politique de confidentialité avec un juriste
- [ ] Choisir la techno deep link (Branch.io vs Firebase Dynamic Links)
- [ ] Définir la page desktop de destination pour les deep links
- [ ] Confirmer la disponibilité des endpoints Odoo 19 listés dans les specs
- [ ] Valider le module coupon/promo compatible Odoo 19
- [ ] Valider la durée de blocage du compte après 5 échecs de PIN (15 min implémenté par défaut, arbitraire — voir §0 module F02)
- [x] ~~Concevoir un signal de disponibilité stock exposable au portail sans ouvrir tout le module stock~~ — fait (`/echango/catalog/stock`, contrôleur en `sudo()`, voir §0), badge "Épuisé" et blocage "Ajouter au panier" actifs. Non testé en réel
- [ ] Panier invité : specs §"Mode invité" prévoit une "commande liée à un partner temporaire Odoo", pas implémenté (F06 ne gère que les comptes connectés, cf. §0) — décider comment créer/retrouver ce partner temporaire sans session Odoo authentifiée avant de s'y attaquer
- [ ] Choisir un service de géocodage inverse (Google Maps / Mapbox / Nominatim) pour le bouton "Utiliser ma position GPS" en F07 — actuellement demande juste la permission (F14) sans remplir l'adresse
- [ ] Gestion de la capacité des créneaux ("créneau complet grisé", QA F07) — les créneaux sont pour l'instant générés côté client sans aucune notion de capacité/réservations existantes ; nécessiterait un modèle back-office dédié
- [ ] Définir au moins une `x_delivery_zone` réelle en back-office (Réglages → Echango Order → Zones de livraison) avant de pouvoir tester un parcours livraison de bout en bout — sinon toute adresse ressort "hors zone"
- [ ] Gestion du fuseau horaire pour `x_creneau` : l'implémentation actuelle envoie l'heure locale de l'appareil telle quelle (`formatOdooDatetime`), sans conversion UTC — à revoir avant une release couvrant plusieurs fuseaux
- [ ] Relier les adresses sauvegardées (F10) au checkout (F07) : proposer de choisir une adresse existante (favorite pré-sélectionnée) plutôt que de ressaisir une adresse à chaque commande — `checkout_controller.py` crée toujours un contact `delivery` ad hoc pour l'instant

## 5. Roadmap macro (rappel — voir `docs/specs_macro_drive_transport.md` §8)

- **Semaines 1-2** : développement app client (catalogue, panier, suivi statuts, bilingue FR/AR)
- **Semaine 3** : tests fonctionnels, validation RTL arabe, corrections
- **Semaine 4** : soumission App Store + Google Play

---

*Mettre à jour ce fichier à chaque avancée significative (changement de statut d'une fonctionnalité, décision prise sur un point de vigilance, etc.).*
