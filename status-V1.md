# Status V1 — Echango Order (MVP Phase 1)

Suivi de l'avancement de l'implémentation. Statuts : `Non démarré` · `En cours` · `Bloqué` · `Terminé`.

Dernière mise à jour : 2026-07-19

**Stratégie en cours** : phase 1 = écrans + navigation complets, sans données ni backend (état local simulé uniquement pour piloter la navigation : session auth). Phase 2 = branchement direct sur Odoo (pas de couche mock intermédiaire).

**⚠️ Changement de stack (2026-07-18)** : le projet a démarré en React Native puis a été **entièrement basculé sur Flutter** en cours de route (choix de l'équipe, expérience Flutter préalable). Tout le code React Native a été supprimé et réécrit en Flutter/Dart — même périmètre fonctionnel (F00-F17), mêmes clés i18n, même logique de navigation, portés dans un nouveau framework. Voir CLAUDE.md § Stack technique pour la note de décision.

**⚠️ Changement PIN (2026-07-18)** : les specs prévoient un PIN à 4 chiffres partout ; décision produit de passer à **6-12 chiffres** (plus d'entropie). Voir CLAUDE.md § Stack technique.

**✅ Validation utilisateur (2026-07-19)** : test manuel complet de l'app (Flutter + Odoo 19 réels, WSL/Docker) sur l'ensemble des fonctionnalités développées à ce jour — **aucune anomalie restante signalée**. Couvre notamment la session de corrections qui a précédé cette validation : badge/remise "Promo" (module `loyalty`/`sale_loyalty`), cohérence des prix panier/catalogue (prix brut forcé, plus de dépendance à la liste de prix), devise réelle affichée au lieu d'un "€" en dur, pagination à la demande + rafraîchissement (tirer vers le bas) sur Accueil/Catalogue. `kListPageSize` repassé de `4` (valeur de test) à **`50`** (valeur définitive) suite à cette validation.

---

## 0. Setup projet

| Tâche | Statut | Notes |
|---|---|---|
| Repo Git créé (`main` + branche de travail) | Terminé | Repo initialisé, docs ajoutés |
| Specs macro & Phase 1 versées dans `docs/` | Terminé | `docs/specs_macro_drive_transport.md`, `docs/specs_phase1_echango_order.md` |
| `CLAUDE.md` rédigé | Terminé | |
| Scaffolding app Flutter | Terminé | `mobile/`, `pubspec.yaml` + `lib/` écrits par Claude Code. `android`/`ios` générés côté utilisateur via `flutter create --org com.echangoorder .` |
| Navigation (go_router : routes publiques + StatefulShellRoute tabs) | Terminé — `flutter analyze` OK | Tous les écrans F00-F17 navigables de bout en bout (`mobile/lib/navigation/app_router.dart`). `flutter analyze` passé côté utilisateur (2 warnings cosmétiques corrigés : import inutile, doc comment) |
| Config i18n FR/AR + RTL | Terminé (base) | `easy_localization`, fichiers `assets/translations/fr.json`/`ar.json`. RTL géré nativement par Flutter (`Directionality` auto sur `Locale('ar')`) — pas de redémarrage app nécessaire contrairement à React Native. Reste à valider visuellement sur device/émulateur |
| État local (session, onboarding) pour piloter la navigation | Terminé | `state/auth_state.dart` (`ChangeNotifier` + `provider`), branché sur le `redirect` de `go_router`, **persisté via `shared_preferences`** (survit au redémarrage de l'app) — sera remplacé par la vraie session Odoo (F02) |
| F14 — Permissions natives réelles (GPS, notifications) | Terminé (code) | `permission_handler` : dialog d'explication + demande système, gestion du refus sans bloquer l'app. **Déclarations natives Android (`AndroidManifest.xml`) et iOS (`Info.plist`/`Podfile`) pas encore ajoutées** — voir CLAUDE.md, à faire après génération de `android/`/`ios/` |
| F17 — Écran substitution produit | Terminé | Accessible via bouton démo depuis le suivi de commande (pas de vrai trigger tant que F11 n'existe pas) |
| F12 — Bouton partage produit | Terminé (partiel) | `share_plus`, sheet natif avec lien placeholder. Réception du deep link non câblée (nécessite domaine + choix de techno, cf. §4) |
| F10 — Suppression de compte (popup + PIN) | Terminé (UI) | `delete_account_dialog.dart` — saisie 6-12 chiffres via `PinInputField`, aucune validation réelle du PIN contre un compte (attend Odoo) |
| Nouvelles dépendances (`permission_handler`, `share_plus`, `shared_preferences`) | Terminé — `flutter analyze` OK | Corrigé suite au 1er `flutter analyze` local : `SharePlus.instance.share(ShareParams(...))` n'existait pas dans la version résolue → remplacé par `Share.share(text)` (API historique, stable). `test/widget_test.dart` généré par `flutter create .` référençait l'ancien template (`MyApp`) → réécrit pour pointer sur `EchangoOrderApp` avec `SharedPreferences.setMockInitialValues({})` |
| Gestion d'erreurs centralisée (codes + i18n) | Terminé (infra) | `lib/errors/` : `AppError` (codes en dot-path mappés 1:1 sur `errors.*` dans les traductions), `AppMessenger` (seul point d'affichage : snackbar erreur/info, dialog bloquant), `ErrorStateView` (état plein écran réutilisable, vides + erreurs). Tous les `ScaffoldMessenger`/messages en dur existants migrés (`coming_soon.dart`, `permission_service.dart`, `MaintenanceScreen`). Convention documentée dans CLAUDE.md — **obligatoire pour tout nouveau code**, y compris le futur client Odoo (mapper les erreurs JSON-RPC vers ces mêmes codes) |
| Validation de formulaire centralisée | Terminé (infra) | `lib/validation/validators.dart` : `validatePhone`, `validatePin` (6-12 chiffres), `validateRequired`, `validatePinMatch` — retournent des `AppError?`, affichées via `AppMessenger` |
| Champ PIN réutilisable | Terminé | `widgets/pin_input_field.dart` — masqué, bascule visibilité, 6-12 chiffres. Câblé (vrais champs + validation) dans Login, RegisterStep1 (téléphone) et RegisterStep3, ChangePin, ForgotPin, DeleteAccount. Aucune vérification contre un vrai compte (attend Odoo) |
| États vides (Cart, OrderHistory, Search) | Terminé | `ErrorStateView` appliqué aux 3 écrans (specs QA : panier vide, aucune commande — avec message spécifique invité, aucun résultat recherche). Boutons démo conservés pour continuer à tester la navigation checkout/suivi/fiche produit tant qu'il n'y a pas de vraies données |
| Numéro de version dans "À propos" | Terminé | `package_info_plus`, F14 QA "Numéro de version visible et à jour" |
| Vérification automatisée des traductions FR/AR | Terminé | `test/translations_completeness_test.dart` — audit demandé suite à un signalement "traductions AR manquantes" (titre "Mon Panier", plusieurs boutons). Audit complet effectué (clés `.tr()` statiques + tous les `screenKey` × title/subtitle) : **aucune clé manquante trouvée** dans le code actuel, fr.json et ar.json parfaitement synchronisés. Seul écart réel trouvé et corrigé : 2 labels "Réf :" en dur (→ `common.reference`). Le signalement initial est probablement un souci de rebuild (assets JSON non rechargés en hot reload) plutôt qu'une vraie traduction manquante — ce test tourne maintenant en continu pour objectiver ça à l'avenir |
| Environnement Odoo 19 + Postgres (Docker/WSL) | Terminé — exécuté et validé côté utilisateur | `backend/docker-compose.yml` (odoo:19 + postgres:16), `backend/config/odoo.conf`, module `backend/addons/echango_order/`. `docker compose up` réel effectué en WSL, base créée, module installé/mis à niveau avec succès. Voir CLAUDE.md § Environnement de dev — backend Odoo |
| Module Odoo F02 — auth téléphone/PIN | Terminé (code) — inscription et connexion validées en réel | `backend/addons/echango_order/` : `models/res_users.py` (champs `x_pin` haché, `x_pin_fail_count`, `x_pin_locked_until` sur `res.users` — voir CLAUDE.md § Principe architecture Odoo), `controllers/auth_controller.py` (`/echango/auth/register`, `/echango/auth/login`). Identité client = utilisateur portail Odoo (`base.group_portal`) lié à un `res.partner`, pas de modèle custom séparé. Connexion branchée sur le point d'extension standard `res.users._check_credentials` (type `pin`, mécanisme utilisé par `auth_oauth`/`auth_ldap`). **Testé en réel via curl contre un Odoo 19 réellement lancé (WSL)** : `/register` puis `/login` retournent `{"success": true, ...}` — 3 aller-retours nécessaires pour corriger des changements internes Odoo 19 découverts en testant (signature de `session.authenticate(env, credential)`, `res.partner.mobile` renommé `phone`, `res.users.groups_id` renommé `group_ids`), tous corrigés et confirmés contre le code source officiel d'Odoo 19. Délai anti brute-force implémenté en fenêtre `locked_until` (pas de `time.sleep`) : 1/2/4/8s puis **15 min de blocage au 5e échec — durée arbitraire, à valider avec le PO** — mécanisme codé mais le scénario de verrouillage (5 échecs consécutifs) n'a pas encore été testé en réel. **Bug corrigé (remonté via F06)** : `register()` écrivait `lang: "fr_FR"` sans vérifier que le français est réellement installé dans Odoo — un compte créé avec une langue non installée faisait planter la création de `sale.order` plus tard ("Invalid language code", mécanisme de notification/mail interne à Odoo qui valide la langue du partenaire). Corrigé : la langue demandée n'est assignée que si elle apparaît dans `res.lang.get_installed()`, sinon `lang=False` (repli automatique standard d'Odoo). **Les comptes de test déjà créés avant ce correctif gardent `lang="fr_FR"` invalide en base** — soit réinscrire un compte de test, soit corriger la langue du contact concerné dans Réglages → Utilisateurs & Sociétés → Contacts |
| Client API JSON-RPC Odoo (Flutter) | Terminé (F02) — **validé en réel par l'utilisateur** (inscription + connexion via l'app, sans erreur) | `lib/services/odoo_api_client.dart` (`register`/`login`, appelle `/echango/auth/*`, cookie de session Odoo gardé en mémoire) + `lib/config/api_config.dart` (URL de base dev, `10.0.2.2` sur émulateur Android). Branché sur `LoginScreen` et `RegisterStep3Screen` (le téléphone saisi à l'étape 1 transite via `extra` de go_router jusqu'à l'étape 3 pour l'appel `register`). Toutes les constantes `AppError` nécessaires existaient déjà (`authInvalidCredentials`, `authPhoneAlreadyUsed`, `authPinLocked`, `validationRequired`, `validationInvalidPin`) — mappage fait dans `odoo_api_client.dart`, aucune traduction à ajouter. Logs backend confirment aussi le délai anti brute-force (tentatives PIN incorrectes court-circuitées par la fenêtre `locked_until` sans même atteindre `res.users.authenticate`). **Limite connue** : cookie de session gardé en mémoire seulement (pas persisté) — perdu au redémarrage de l'app ; le stockage sécurisé (`flutter_secure_storage`, voir ligne juste en dessous) reste à faire avant une vraie gestion de session 24h. `_rpc()` généralisé pour supporter aussi le `/web/dataset/call_kw` standard (voir F03 ci-dessous), pas seulement les 2 endpoints custom d'auth |
| F03 — Accueil : affichage produits (Flutter + backend) | **Terminé — validé en réel** | **Backend** : `security/ir.model.access.csv` + `security/ir_rule.xml` dans `echango_order` — le groupe portail n'avait par défaut AUCUN accès à `product.template`/`product.category` (vérifié contre le code source Odoo 19 des modules `product` et `sale`, aucune entrée portail trouvée), donc accès lecture seule accordé + `ir.rule` restreignant aux produits `sale_ok = true` (pas de fuite du catalogue interne). **Flutter** : `HomeScreen` réécrit — `OdooApiClient.searchRead()` (`search_read` standard via `call_kw`, pas de contrôleur custom) sur `product.template` (`name`, `list_price`, `image_128`), grille 2 colonnes, état vide et état d'erreur via `ErrorStateView` existant. **Écart assumé vs spec Expert Odoo** : filtre `sale_ok = true` à la place de `is_published` (champ qui n'existe pas sans le module `website_sale`, non installé) — pas de notion de "produits mis en avant" pour l'instant, tous les produits vendables s'affichent ; la curation viendra avec `x_vitrine_publique` (F00) si besoin. Piège rencontré en testant : `docker compose exec odoo odoo -u ...` contourne l'entrypoint de l'image officielle (qui traduit `HOST`/`USER`/`PASSWORD` en paramètres de connexion) — nécessite `--db_host=db --db_user=odoo --db_password=odoo` explicites pour cette commande ponctuelle (le `docker compose restart odoo` normal repasse bien par l'entrypoint, lui) |
| F04 — Catalogue & Recherche (Flutter + backend) | Terminé (code), non re-testé après correction | **`qty_available` retiré (3e itération)** : accordé l'accès `product.product` (voir historique précédent) pensant que ça suffirait, mais le calcul de `qty_available` va ENSUITE lire `stock.warehouse` (et probablement encore plus loin — quants, emplacements) : nouvelle `AccessError` remontée par l'utilisateur. Plutôt que de continuer à ouvrir l'accès module de stock par module (surexposition de la mécanique interne d'inventaire à un compte client, contraire à CLAUDE.md § Sécurité), **`qty_available` est retiré des champs demandés** en Catalogue/Recherche — le badge "Épuisé" du `ProductGridTile` reste dans le code mais restera inactif (`qty` toujours `null`) tant qu'on n'aura pas un vrai signal de disponibilité, plus étroit, à concevoir séparément (probablement au moment de F07/checkout `checkout.out_of_stock`, pas dans cette passe). L'accès `product.product` + son `ir.rule` ajoutés pour la 2e itération sont **retirés** de `security/` (redevenus inutiles) — un nouveau `-u echango_order` les supprimera de la base (Odoo nettoie les enregistrements dont l'external ID a disparu du module). **Flutter** : `CategoryProductsScreen` (grille filtrée `categ_id`, nom de catégorie transmis via `extra` de go_router depuis `CatalogScreen`), `SearchScreen` (`name ilike`, anti-rebond 400ms via `Timer`, pas de nouvelle dépendance). Nouveau widget partagé `lib/widgets/product_grid_tile.dart` (`ProductGridTile`) utilisé par Accueil/Catalogue/Recherche — évite de dupliquer 3 fois le décodage d'image + la mise en page ; badge "Épuisé" (`qty_available <= 0`) et bouton "+" optionnel (absent sur l'Accueil, présent en Catalogue/Recherche comme dans le wireframe) qui appelle `showComingSoon` (ajout réel au panier = F06, pas fait). Nouvelles clés `catalog.outOfStock` + `emptyStates.categories{Title,Message}` en FR/AR. **Bug signalé par l'utilisateur (capture d'écran) et corrigé** : `CatalogScreen` faisait un `search_read` direct sur `product.category`, ce qui remontait aussi les catégories techniques par défaut d'Odoo (Dépenses, Achats, Construction...) sans aucun produit vendable dedans. Remplacé par un `read_group` sur `product.template` (`groupby: categ_id`) — nouvelle méthode `OdooApiClient.readGroup()` — qui ne fait remonter que les catégories ayant réellement des produits visibles côté portail, avec le nombre de produits par catégorie en bonus. `security/ir.model.access.csv` gardé tel quel (accès `product.category` toujours nécessaire : Odoo doit lire ce modèle en interne pour résoudre le nom affiché du many2one `categ_id`, même sans `search_read` direct dessus depuis Flutter). **Non fait dans cette passe** : barre de recherche accessible depuis l'Accueil (QA F03, jamais implémentée, risque de complications de navigation cross-onglet avec `StatefulShellRoute` — à traiter séparément si besoin) |
| F04 — nettoyage du warning `read_group` déprécié | Terminé | `read_group` (utilisé pour les catégories du catalogue) affichait `DeprecationWarning: Since 19.0, read_group is deprecated` dans les logs Odoo à chaque chargement de l'écran Catalogue — pas une erreur, mais un signal que la méthode sera retirée dans une future version. Migré vers `formatted_read_group` (vérifié contre le code source, `addons/web/models/models.py`) : même représentation `[id, nom]` pour `categ_id`, mais le comptage n'est plus implicite — demandé explicitement via `aggregates: ['__count']` (valeur par défaut de `OdooApiClient.readGroup()`), clé de résultat `__count` au lieu de `categ_id_count` |
| F05 — Fiche Produit (Flutter + backend) | Terminé (code), non testé en réel | **Backend** : accès portail ajouté pour `uom.uom` (`security/ir.model.access.csv`) — vérifié au préalable contre le code source Odoo 19 (`addons/uom/security/ir.model.access.csv`) qu'aucune entrée portail n'existait déjà, comme pour `product.category`/`product.template` en F03 ; nécessaire pour résoudre le nom affiché du many2one `uom_id`. Pas de nouvel `ir.rule` (unités de mesure non sensibles, pas de filtrage nécessaire). Nécessite `-u echango_order`. **Flutter** : `ProductDetailScreen` réécrit — `OdooApiClient.read()` (nouvelle méthode, `read` standard par id, plus direct qu'un `search_read` avec domaine `id=`), champs `name`/`description`/`list_price`/`image_1920`/`uom_id`. `description` est un champ **Html** côté Odoo 19 (vérifié contre le code source, pas un `Text`/`Char` comme les specs le suggéraient implicitement) — balises retirées côté client par une regex plutôt que d'ajouter une dépendance de rendu HTML pour ça. Sélecteur de quantité local (min 1), bouton "Ajouter au panier" branché sur F06 (voir ligne ci-dessous). Bouton de partage F12 existant conservé tel quel. **`qty_available` volontairement absent** (même décision qu'en F04) : le bouton "Ajouter au panier" reste toujours actif, critère QA "produit épuisé → bouton désactivé" différé — voir point de vigilance §4 |
| F06 — Panier (Flutter + backend) | Terminé (code), non testé en réel | **Backend** : `controllers/cart_controller.py` (nouveau) — `/echango/cart`, `/echango/cart/add`, `/echango/cart/update`, `/echango/cart/remove`. Vérifié au préalable contre le code source Odoo 19 (`addons/sale/security/ir.model.access.csv` + `ir_rules.xml`) : le groupe portail a déjà un accès **lecture seule** à `sale.order`/`sale.order.line` (`1,0,0,0` — ni write, ni create, ni unlink), une limite de sécurité **volontaire** d'Odoo (pas un oubli de config comme pour le catalogue), pour empêcher un client de manipuler directement des documents de vente via l'ORM. D'où un contrôleur custom (comme F02) plutôt qu'un élargissement des droits ORM bruts : il retrouve/crée le devis brouillon du partenaire connecté, vérifie la propriété de chaque ligne avant modification/suppression, et agit en `sudo()`. Le panier EST le devis Odoo (`sale.order` état `draft`) — pas un état local à resynchroniser plus tard, conforme à CLAUDE.md § Principe architecture Odoo. Aucun nouveau fichier `ir.model.access`/`ir.rule` nécessaire (le contrôleur fait tout en `sudo()`, Flutter n'appelle jamais `sale.order`/`sale.order.line` en direct). **Flutter** : `state/cart_state.dart` (`CartState`, `ChangeNotifier` comme `AuthState`), 4 nouvelles méthodes sur `OdooApiClient`, `CartScreen` réécrit (liste, +/-, suppression, sous-total/total), badge sur l'onglet Panier (`main_tab_scaffold.dart`, widget Material `Badge` standard). Ajout au panier branché sur `ProductDetailScreen` (avec la quantité choisie) et sur le bouton "+" du `ProductGridTile` (Catalogue/Recherche). Nouveau `utils/require_account.dart` : un invité qui tente d'ajouter au panier se voit proposer l'inscription (`actions.signUpToOrder`, traduction déjà prête mais jamais branchée jusqu'ici) plutôt que d'appeler un endpoint `auth='user'` qui échouerait. **Panier invité non implémenté** (commande liée à un partner temporaire Odoo, cf. specs §"Mode invité" — décision produit déjà notée comme ouverte en §4, pas traitée dans cette passe) : un invité voit l'état "panier vide" existant, sans appel API. Nouvelle erreur `cart.product_unavailable` mappée vers la constante `AppError.checkoutOutOfStock` déjà existante (même message, pas de nouvelle traduction) ; erreurs "ligne introuvable" mappées vers `AppError.notFound` (déjà existante) |
| Persistance de session (Flutter) | Terminé (code), non testé en réel | `flutter_secure_storage` ajouté (Keychain/Keystore, cf. CLAUDE.md § Sécurité). `OdooApiClient` : `restoreSession()` (lu au démarrage dans `main()`, avant `runApp`, avec `AuthState`/`CheckoutState` désormais créés dans `main()` plutôt que dans le `State` de l'app pour permettre ce `await` préalable), persistance du cookie à chaque réponse (`_rpc()`), `clearSession()` (appelé au logout, `ProfileScreen`). Pas de vérification proactive de validité au démarrage (pas d'appel "ping") : callback `onSessionExpired` déclenché par `_rpc()` quand le mapping d'erreur donne `AppError.authSessionExpired`, qui appelle `AuthState.logout()` — le `redirect` de go_router (déjà branché sur `AuthState`) renvoie alors automatiquement vers les routes publiques. `test/widget_test.dart` mis à jour (nouvelle signature `EchangoOrderApp`) |
| Signal de disponibilité stock (Flutter + backend) | Terminé (code) — **validé en réel, bug corrigé** | **Backend** : `controllers/catalog_controller.py` (nouveau) — `/echango/catalog/stock`, calcule `qty_available` en `sudo()` sur une liste d'ids (`product.template`, filtré `sale_ok`) sans exposer `product.product`/`stock.warehouse` au portail. `cart_controller.py.add()` valide aussi le stock côté serveur (pas seulement l'UI) avant d'ajouter une ligne, réutilise `cart.product_unavailable`. **Flutter** : `OdooApiClient.getStock()`, appelé en 2e temps après le chargement des produits (fusionné dans les maps produit sous la clé `qty_available` déjà attendue par `ProductGridTile`, donc le badge "Épuisé" s'active sans changement côté widget). `AppButton.onPressed` rendu nullable (`VoidCallback?`) pour pouvoir désactiver "Ajouter au panier" + message `errors.checkout.out_of_stock` sur la fiche produit quand épuisé — critère QA F05 enfin couvert. **Bug signalé par l'utilisateur et corrigé** : `getStock()` n'était appelé que dans `CategoryProductsScreen`/`SearchScreen`/`ProductDetailScreen`, pas dans `HomeScreen` (F03) — le badge "Épuisé" n'apparaissait donc jamais sur l'Accueil. `HomeScreen._fetchProducts()` complété avec le même appel `getStock()` |
| F07 — Checkout & Mode de Réception (Flutter + backend) | Terminé (code), non testé en réel | **Backend** : `models/sale_order.py` (`x_reception_mode` Selection, `x_creneau` Datetime — début du créneau, durée fixe 2h par convention, pas de champ de fin séparé) ; `models/delivery_zone.py` (nouveau modèle `x_delivery_zone` : name/city/zip_code, géré par un menu back-office propre "Echango Order" ajouté par `views/delivery_zone_views.xml`, accès réservé aux utilisateurs internes — **pas exposé au portail**, la vérification de zone passe par un contrôleur en `sudo()`). `controllers/checkout_controller.py` (nouveau) : `/echango/checkout/check_zone` (ville/code postal vs `x_delivery_zone`, matching simple `=ilike`/`=` — pas de normalisation avancée) et `/echango/checkout/confirm` (fixe le mode/l'adresse (nouveau contact `type=delivery`)/le créneau puis `action_confirm()` — méthode standard vérifiée contre le code source `addons/sale/models/sale_order.py`, pas une écriture brute de `state`). **Flutter** : `state/checkout_state.dart` (`CheckoutState`, éphémère, non persisté, remis à zéro à chaque entrée dans le tunnel depuis `CartScreen` et après confirmation), `utils/timeslots.dart` (créneaux fixes de 2h générés côté client — 10h/14h/16h aujourd'hui, 9h/11h demain — **pas de notion de créneau complet**, aucun modèle de capacité en back-office pour l'instant). Les 4 écrans réécrits : `CheckoutReceptionModeScreen` (choix réel), `CheckoutAddressScreen` (formulaire + vérification de zone + bouton GPS qui redemande la permission F14 mais **n'autoremplit pas** l'adresse — géocodage inverse nécessiterait un service externe, Google/Mapbox/Nominatim, décision non prise, `showComingSoon` pour cette seule partie), `CheckoutOutOfZoneScreen` (bascule vers retrait), `CheckoutTimeslotScreen` (sélection), `CheckoutSummaryScreen` (récapitulatif + appel réel de confirmation, panier rafraîchi ensuite — redevient vide puisque le devis n'est plus à l'état brouillon). Nouvelles clés `checkout.{streetLabel,cityLabel,zipLabel,notesLabel,today,tomorrow,paymentOnDelivery,itemsLabel}` en FR/AR |
| F08 — Confirmation (écran, Flutter) | Terminé (partiel, code) | `OrderConfirmationScreen` affiche maintenant les vraies données renvoyées par `/echango/checkout/confirm` (référence, montant, mode, créneau) transmises via `extra` de go_router, plus le `orderRef` en path param. **Suivi de commande en temps réel (statuts synchronisés + notifications push) hors scope de cette passe** — `order_tracking_screen.dart` affiche maintenant les vraies données de la commande (F09) mais pas d'étapes préparation/livraison synchronisées, ça reste un simple état Odoo (`state`) |
| F09 — Historique & Reorder (Flutter + backend) | Terminé (code), non testé en réel | **Backend** : `cart_controller.py.reorder()` (nouveau) — recopie les lignes d'une commande passée dans le panier en cours, exclut les produits non vendables/en rupture (même logique que `add()`), retourne la liste des noms exclus. Aucun nouvel accès nécessaire : `sale.order`/`sale.order.line` déjà lisibles par le portail pour ses propres commandes (règle standard `sale_order_rule_portal`/`sale_order_line_rule_portal`, vue en F06) — juste `search_read` classique pour la liste et le détail, pas de contrôleur custom pour la lecture. **Flutter** : `OdooApiClient.searchRead()` gagne un paramètre `order` (tri), `CartState.reorder()` (applique le panier renvoyé + expose la liste des indisponibles). `OrderHistoryScreen` et `OrderTrackingScreen` réécrits avec les vraies données. Popup de confirmation + avertissement si produits exclus + redirection vers le panier, conforme aux critères QA |
| F10 — Profil : en-tête réel + modification nom + PIN (Flutter + backend) | Terminé (code), non testé en réel | **Backend** : `controllers/profile_controller.py` (nouveau) — `/echango/profile` (lecture `name`/`phone`/`lang`/`partner_latitude`/`partner_longitude`), `/echango/profile/update_name`, `/echango/profile/change_pin`. Vérifié au préalable contre le code source Odoo 19 (`addons/base/security/ir.model.access.csv`) : `res.partner` est **lecture seule** pour le portail (`1,0,0,0`), même limite volontaire que `sale.order`/`sale.order.line` (F06) — d'où un contrôleur `sudo()` dédié plutôt qu'un élargissement des droits ORM. Aucun nouveau modèle/champ/fichier `security` (le contrôleur ne touche que des champs standards déjà en base) : **pas de `-u echango_order` nécessaire pour ce stage, un `docker compose restart odoo` suffit**. `change_pin` réutilise directement `res.users._check_pin`/`_set_pin` (F02) plutôt que de dupliquer la vérification — effet secondaire assumé : un mauvais PIN actuel pendant un changement compte aussi dans le compteur anti brute-force existant. **Bonus documentaire** : en explorant `res.partner` pour ce stage, confirmation que `partner_latitude`/`partner_longitude` sont des champs standards du module `base` lui-même (pas besoin de `base_geolocalize`) — `CLAUDE.md` mis à jour, `x_latitude`/`x_longitude` retirés de la liste des champs custom (même traitement que `x_langue` précédemment). **Flutter** : `OdooApiClient.getProfile()`/`updateProfileName()`/`changePin()`, `ProfileScreen` réécrit (en-tête nom/téléphone réel via `FutureBuilder`, tap sur le nom → `AlertDialog` d'édition), `ChangePinScreen` réécrit (branché sur le vrai endpoint, remplace le `showComingSoon`). **Téléphone volontairement en lecture seule** : c'est aussi l'identifiant de connexion (F02), le modifier nécessiterait une re-vérification par SMS non implémentée. Nouvelles clés `profile.{pinChanged,editNameTitle,nameUpdated}` en FR/AR. **Reste pour F10** : adresses (CRUD `res.partner` enfants type `delivery`), Ma Localisation (GPS réel + `partner_latitude`/`longitude`), suppression de compte réelle (actuellement UI seule, voir ligne plus haut), notifications (petit écran d'état des permissions) |
| F10 — Adresses sauvegardées (Flutter + backend) | Terminé (code), non testé en réel | **Backend** : nouveau modèle `models/res_partner.py` (`_inherit = "res.partner"`, champ `x_adresse_favorite` Boolean) — implémente exactement le champ déjà pré-approuvé dans CLAUDE.md § Custom fields Odoo attendus (Odoo n'a pas de notion de "default address" parmi les contacts enfants d'un partenaire). **Nécessite `-u echango_order`** (nouveau champ = nouvelle colonne DB), contrairement au stage précédent. `controllers/profile_controller.py` complété avec `list_addresses`/`add_address`/`update_address`/`remove_address` — adresses = contacts enfants standards de `res.partner` (`parent_id` + `type='delivery'`, même mécanisme que celui déjà utilisé côté checkout F07), toujours en `sudo()` avec vérification explicite de propriété (`parent_id` = partenaire courant) puisque `res.partner` reste en lecture seule pour le portail. Une seule adresse favorite à la fois : `_unset_other_favorites()` désactive les autres favoris du même parent avant d'en activer un nouveau. **Flutter** : `OdooApiClient.listAddresses/addAddress/updateAddress/removeAddress`, `AddressesScreen` réécrit (liste + carte par adresse avec icône étoile si favorite + suppression avec confirmation) et nouveau `_AddressFormScreen` (ajout/modif, champs nom/rue/ville/code postal/notes + case à cocher favorite). Nouvelles clés `common.delete` + section `addresses.*` en FR/AR. **Non fait dans cette passe** : le checkout (F07) ne propose pas encore de choisir une adresse sauvegardée — il continue de créer un contact `delivery` ad hoc à chaque confirmation (`checkout_controller.py` inchangé) ; réutiliser une adresse sauvegardée (et sa favorite par défaut) au checkout reste un point de vigilance à traiter séparément |
| F10 — Ma Localisation (Flutter + backend) | Terminé (code), non testé en réel | **Backend** : `controllers/profile_controller.py` complété avec `/echango/profile/update_location`, écrit sur les champs standards `res.partner.partner_latitude`/`partner_longitude` (déjà confirmés natifs du module `base`, voir stage 1) — aucun nouveau champ/modèle, un simple `docker compose restart odoo` suffit pour ce stage. **Flutter** : nouveau paquet `geolocator` (`pubspec.yaml`) — lit la position GPS réelle une fois la permission système accordée, aucune clé d'API externe nécessaire (pas de géocodage inverse ici, juste lat/lng brutes). `permission_service.dart` : `requestPermissionWithExplanation`/`requestLocationPermission`/`requestNotificationPermission` renvoient désormais `Future<bool>` (accordée ou non) au lieu de `Future<void>`, pour permettre à l'appelant d'enchaîner une action réelle après une permission accordée (jusqu'ici, aucun appelant n'en avait besoin) — les appels existants (`RegisterStep2`, `CheckoutAddress`, `OrderConfirmation`) restent inchangés, un `Future<bool>` ignoré est toujours valide en contexte `void`. `MyLocationScreen` réécrit : affiche la position déjà enregistrée (`getProfile()`), bouton "Utiliser ma position GPS" qui demande la permission (F14) puis `Geolocator.getCurrentPosition()` puis sauvegarde immédiatement côté Odoo. **Erreur "service de localisation désactivé"** réutilise `AppError.permissionDenied` (même message que permission refusée — pas un nouveau code pour la même idée UX "continuez sans, ou activez plus tard"). Nouvelles clés `myLocation.{currentLabel,none,updated}` en FR/AR. **Non vérifié dans ce sandbox** : `geolocator: ^13.0.1` choisi à partir de la dernière version stable connue au moment de l'écriture — à confirmer/ajuster via `flutter pub get` côté utilisateur (le réseau du sandbox ne permet pas de vérifier la disponibilité de cette version sur pub.dev) |
| F10 — Suppression de compte réelle + Notifications (Flutter + backend) | Terminé (code), non testé en réel | **Backend** : `controllers/profile_controller.py` complété avec `/echango/profile/delete_account` — réutilise `res.users._check_pin` (F02), puis `write({"active": False})` : suppression logique standard Odoo, `active` est un champ déjà présent nativement sur `res.users` (mixin standard), **aucun nouveau champ custom** (conforme à la spec Expert Odoo "write sur res.users, champ active = false"). `docker compose restart odoo` suffit. **Invalidation de session durcie** : en plus de `active=False`, un `password` aléatoire est écrit (`secrets.token_urlsafe(32)`) — champ de sécurité standard dont dépend le token de session Odoo, ce qui force l'expiration immédiate de toute session déjà ouverte sur un autre appareil (voir § Points de vigilance). Comportement HTTP réel encore à confirmer côté utilisateur (pas de Docker dans le sandbox). **Confirmation par SMS après suppression (specs QA) hors scope**, aucun fournisseur SMS choisi. **Flutter** : `OdooApiClient.deleteAccount()`, `delete_account_dialog.dart` réécrit — appelle l'endpoint réel, efface la session (`clearSession()`) et déconnecte (`AuthState.logout()`) au lieu du `showComingSoon` précédent ; le `redirect` de go_router renvoie automatiquement vers les routes publiques. `NotificationSettingsScreen` réécrit : F11 (FCM) n'existe pas encore, donc pas de préférence à écrire côté Odoo — l'écran affiche/gère uniquement l'état de la permission système notifications (F14, `permission_handler`), avec redirection vers les réglages du téléphone si refusée définitivement (`openAppSettings()`). Nouvelles clés `notificationSettings.{enabled,disabled,openSettings}` en FR/AR. **F10 est maintenant complet côté code** (en-tête/nom/PIN, adresses, localisation, suppression de compte, notifications) — reste non testé en réel (`flutter analyze`/`flutter run` côté utilisateur) |
| F16 — Annulation Commande (Flutter + backend) | Terminé (code), non testé en réel | **Backend** : nouveau `controllers/order_controller.py` — `/echango/order/cancel`, vérifie la propriété (`partner_id`) puis appelle `action_cancel()` (méthode standard vérifiée contre le code source Odoo 19 `addons/sale/models/sale_order.py` : passe l'état à `cancel`, annule les factures brouillon liées, lève `UserError` si la commande est verrouillée — capturé et mappé vers `order.cannot_cancel`). Aucun nouveau modèle/champ, `docker compose restart odoo` suffit. **Simplification assumée pour la règle "annulation possible uniquement avant préparation"** (specs QA) : pas de suivi `stock.picking` synchronisé (F08 différé, voir plus haut) donc le seul proxy disponible est l'état `sale.order` lui-même — le bouton n'est affiché que tant que `state == 'sale'` ("Confirmée"), pas de fenêtre de délai distincte. **Flutter** : `OdooApiClient.cancelOrder()`, `order_tracking_screen.dart._confirmCancel()` branché sur l'appel réel (au lieu de `showComingSoon`) — succès : rafraîchit l'écran (statut passe à "❌ Annulée", bouton disparaît) + toast `order.cancelled`. Nouvelle clé `order.cancelled` en FR/AR (`errors.order.cannot_cancel` existait déjà). **Notification push après annulation (specs QA) hors scope**, dépend de F11 |
| F17 — Substitution Produit (Flutter + backend) | Terminé (code), non testé en réel | **Backend** : `models/sale_order.py` complété avec `SaleOrderLine.x_substitution_produit` (Many2one `product.product`, domaine `sale_ok=True`) — champ déjà pré-approuvé dans CLAUDE.md § Custom fields Odoo attendus (aucune notion de substitution dans Odoo standard). Nouveau `views/sale_order_views.xml` : hérite `sale.view_order_form`, ajoute la colonne dans la liste des lignes de commande — le signalement de rupture + la suggestion restent **manuels côté préparateur en back-office** (specs Expert Odoo "Phase 1 manuelle"), pas d'automatisation stock. **Nécessite `-u echango_order`** (nouveau champ + nouvelle vue). `controllers/order_controller.py` complété avec `/echango/order/substitution` (lecture, résout le nom/prix du produit suggéré en `sudo()` — `product.product` n'est pas exposé au portail, voir F04), `/echango/order/substitution/accept` (remplace `product_id` de la ligne — `price_unit`/`name` sont des champs calculés stockés qui se recalculent automatiquement, vérifié contre le code source Odoo 19) et `/echango/order/substitution/refuse` (supprime la ligne). **Flutter** : `OdooApiClient.getSubstitution/acceptSubstitution/refuseSubstitution`, `SubstitutionScreen` réécrit avec les vraies données (nom/prix réels) au lieu des valeurs figées ("Lait entier 1L"...), `OrderTrackingScreen` n'affiche le bouton d'entrée que si une substitution est réellement en attente (`hasSubstitution`), au lieu d'être un bouton démo permanent. **Hors scope, dépend de F11** : notification push déclenchant l'écran (remplacé par le bouton conditionnel du suivi de commande) et substitution automatique après 30 min sans réponse (specs QA) — aucun mécanisme de délai/timer implémenté |
| F00 — Vitrine publique (Flutter + backend) | Terminé (code), non testé en réel | **Backend** : nouveau `models/product_template.py` (`x_vitrine_publique`, Boolean — champ déjà pré-approuvé dans CLAUDE.md, aucun équivalent standard sans `website_sale`). Nouveau `views/product_template_views.xml` (hérite `product.product_template_only_form_view`) pour que la sélection des produits vitrine soit configurable depuis le back-office standard (specs Expert Odoo). Nouveau `controllers/vitrine_controller.py` — `/echango/vitrine/products`, **`auth='public'`** (pas `auth='user'`) : un visiteur de la vitrine n'a même pas de session portail (contrairement au reste de l'app), c'est le premier endpoint de ce type. Filtré `sale_ok` ET `x_vitrine_publique`, en `sudo()` — pas d'ouverture d'accès ORM à `base.group_public`, même logique de non-surexposition que pour le portail. **Nécessite `-u echango_order`** (nouveau champ + vue). **Flutter** : `OdooApiClient.getVitrineProducts()` (fonctionne sans cookie de session, `_rpc()` n'en envoie que s'il y en a un). `VitrineScreen` réécrit : grille réelle (réutilise `ProductGridTile` de F04), taper une tuile ou son bouton "+" ouvre le popup d'inscription (specs UX) plutôt qu'une fiche produit — le wireframe F00 n'en prévoit pas pour un visiteur sans compte. Popup avec 3 choix : "S'inscrire" → `/register/step1` direct, "Se connecter" → `/login` direct, "Plus tard" → ferme le popup. Nouvelle section `vitrine.{popupTitle,popupBody}` en FR/AR |
| F13 — Pages Légales (Flutter, contenu statique) | Terminé (texte provisoire) | Specs Expert Odoo : "aucun appel API — contenu statique intégré dans l'app", donc pas de backend pour cette fonctionnalité. `legal_document_screen.dart` réécrit : contenu réel (CGU, confidentialité, mentions légales, cookies) au lieu du simple libellé affiché précédemment, avec numéro de version + date de mise à jour visibles, bandeau `legal.draftNotice` rappelant que **le texte est provisoire, à faire valider par un juriste avant soumission aux stores** (décision explicite de l'utilisateur : texte de remplissage plutôt qu'écrans vides). Nouvelle route top-level `/legal/:docType` (ajoutée à `_publicPaths` dans `app_router.dart`) en plus de `/profile/legal/:docType` déjà existante — nécessaire pour que les CGU/confidentialité soient accessibles **depuis l'inscription** (specs QA), où l'utilisateur n'est pas encore authentifié et ne peut pas atteindre les routes nichées sous `/profile`. `register_step3_screen.dart` (étape de création effective du compte) affiche désormais un rappel + liens directs CGU/confidentialité (`auth.termsNotice`) — **pas de case à cocher bloquante**, les specs QA demandent seulement que les pages soient "accessibles", pas une acceptation explicite gate-keeper. `about_screen.dart` complété avec le lien "Cookies" (seuls CGU/Confidentialité/Mentions légales y étaient jusqu'ici). Nouvelles clés `legal.{version,updatedOn,draftNotice,docs.*}` + `auth.termsNotice` en FR/AR |
| F15 — Code Promo (Flutter + backend) | Terminé (code), non testé en réel | **Décision "standard avant custom" appliquée à 100%** : la spec Expert Odoo évoquait `sale.coupon.program` (nom de modèle qui n'existe plus — remplacé par le module `sale_loyalty`/`loyalty` en Odoo 17+, vérifié contre le code source Odoo 19 avant tout code). Nouvelle dépendance `"sale_loyalty"` ajoutée au manifest (LGPL-3, Community Edition, confirmé — pas besoin d'Enterprise). **Aucun champ custom** : le module standard couvre exactement le besoin (code, réduction, date d'expiration, nombre d'utilisations restantes). **Backend** : `controllers/checkout_controller.py` complété avec `/echango/checkout/apply_promo` — réutilise `sale.order._try_apply_code()` (méthode standard) puis applique automatiquement l'unique récompense réclamable via `_apply_program_reward()` (un programme "code promo" bien configuré n'a qu'une récompense ; pas d'écran de choix multiple, hors scope du wireframe). **Un seul code par commande** (specs QA) : le contrôleur retire l'éventuel code déjà appliqué (`applied_coupon_ids`/`code_enabled_rule_ids` vidés + `_update_programs_and_rewards()`) avant d'en essayer un nouveau, plutôt que de les cumuler. **Distinction invalide/expiré/déjà utilisé sans dépendre du texte des messages internes** (leçon retenue de F02 `AccessDenied`, dont le message est aussi peu fiable — ici en plus le texte Odoo est traduit selon la langue du client, donc totalement impropre à servir de code) : `_resolve_promo_error()` reproduit les vérifications internes de `_try_apply_code` (vérifiées ligne à ligne contre le code source) pour renvoyer nos codes `AppError` stables. `cart_controller.py._cart_payload()` modifié : les lignes de récompense (`is_reward_line`, champ standard) sont exclues de la liste produit (pas de +/- quantité ni bouton supprimer sur une ligne de réduction) et un nouveau champ `discount` (`order.reward_amount`, standard, déjà négatif) est renvoyé séparément. **Nécessite `-u echango_order`** (nouvelle dépendance de module à installer). **Flutter** : `OdooApiClient.applyPromoCode()`, `CartState.discount` + `applyPromoCode()`, `CheckoutSummaryScreen` complété avec le champ code promo + bouton (wireframe), ligne "Réduction" conditionnelle et récapitulatif sous-total/réduction/total. Nouvelles clés `checkout.{promoCodeLabel,promoAppliedPrefix,discountLabel}` en FR/AR (`errors.promo.*` existaient déjà depuis le début du projet, jamais câblées jusqu'ici). **⚠️ Configuration back-office requise avant de pouvoir tester** : créer un programme dans Ventes → Réductions & Fidélité (ou "Coupons & cartes de fidélité" selon la traduction du menu) avec **Type de programme = "Discount Code"**, y définir manuellement le champ `code` de la règle (ex. "BIENVENUE" à la place de l'UUID généré par défaut), une seule récompense (ex. réduction fixe), et éventuellement `date_from`/`date_to` + `limit_usage`/`max_usage` — sinon tout code testé renverra `promo.invalid` |
| Qualité clients — vérification manuelle des nouveaux comptes (Flutter + backend) | Terminé (code), non testé en réel | **Décision produit** (suite à échange avec l'utilisateur) : plutôt qu'un service de géocodage externe pour valider les adresses, un modérateur valide manuellement chaque nouveau compte client en back-office avant qu'il puisse commander. **Backend** : `models/res_partner.py` — `x_verification_state` (Selection `pending`/`verified`/`rejected`), **défaut `verified` au niveau du champ** pour ne pas affecter les partenaires déjà en base (adresses enfants, contacts internes...) ; `/echango/auth/register` positionne explicitement `pending` sur un **nouveau** compte client uniquement. `views/res_partner_views.xml` (nouveau) : statusbar + boutons "Valider"/"Rejeter" sur la fiche contact standard (`base.view_partner_form`), + menu "Clients à valider" (Echango Order) filtré sur les comptes portail non vérifiés. `controllers/checkout_controller.py.confirm()` bloque la confirmation si le compte n'est pas `verified` (`auth.account_pending_verification`/`auth.account_rejected`). `cart_controller.py._cart_payload()` renvoie aussi `verification_state` pour avertir dès le Panier. **Nécessite `-u echango_order`** (nouveau champ + vue). **Flutter** : nouveaux codes `AppError.authAccountPendingVerification`/`authAccountRejected`, `CartState.verificationState`, `CartScreen` affiche un message et désactive "Valider mon panier" si le compte n'est pas vérifié (la vérification qui compte reste côté serveur à la confirmation). **⚠️ Migration** : après `-u echango_order`, tous les comptes déjà créés restent `verified` par défaut (rien à faire) — seuls les comptes inscrits **après** cette mise à jour démarrent `pending` et nécessitent une validation manuelle (Réglages → Contacts, ou menu "Echango Order → Clients à valider") avant de pouvoir passer commande. **Hors scope pour l'instant** : notification/activité automatique pour prévenir le modérateur qu'un nouveau compte attend validation (le modérateur doit consulter le menu manuellement) |
| F10 — "Ma Localisation" fusionnée dans "Mes adresses" (Flutter + backend, suite retour utilisateur) | Terminé (code), non testé en réel | **Décision produit** (suite à échange avec l'utilisateur) : plutôt que deux entrées séparées du menu Profil pour un besoin qui se recoupe (indiquer où livrer), le GPS devient une **option par adresse** plutôt qu'un point unique au niveau du profil. **Supprimé** : `MyLocationScreen`, la route `/profile/my-location`, l'entrée "Ma Localisation" dans `ProfileScreen`, `/echango/profile/update_location`, `OdooApiClient.updateLocation()`, `latitude`/`longitude` de `get_profile()`, les traductions `screens.MyLocation.*`/`myLocation.*` devenues orphelines. **Backend** : `profile_controller.py._address_payload/add_address/update_address` étendus avec `latitude`/`longitude` (toujours `res.partner.partner_latitude`/`partner_longitude`, standards, maintenant utilisés sur les contacts enfants `type='delivery'` plutôt que sur le partenaire principal). Aucun nouveau champ, `docker compose restart odoo` suffit pour ce changement précis (mais un `-u echango_order` reste dû pour les stages précédents non encore appliqués). **Choix assumé : le GPS reste un complément optionnel à la saisie manuelle (rue/ville/code postal), pas un remplacement** — sans service de géocodage inverse choisi, impossible de déduire rue/ville/code postal depuis des coordonnées seules, et la vérification de zone de livraison (F07) continue de se baser sur ville/code postal. **Flutter** : `_AddressFormScreen` (dans `addresses_screen.dart`) gagne un bouton "Utiliser ma position GPS" (réutilise `geolocator`/`permission_service.dart` déjà en place) qui capture et affiche les coordonnées, envoyées avec le reste du formulaire. `0.0`/`0.0` (défaut Odoo pour un Float jamais renseigné) traité comme "pas de position" côté app plutôt qu'une vraie coordonnée. **Reste un point de vigilance inchangé** : relier une adresse sauvegardée (GPS ou non) au checkout (F07) — toujours pas fait |
| Badge "Promo" sur les tuiles produit (Flutter + backend) | **Terminé — validé en réel (2026-07-19)**, badge + remise appliqués correctement au panier et à la commande confirmée | Suite à une question de l'utilisateur sur la gestion des promos dans Odoo : réutilise entièrement le module standard `loyalty` (déjà en place pour F15, aucun nouveau champ/modèle custom — bon exemple de CLAUDE.md § Principe architecture Odoo). **Backend** : `controllers/catalog_controller.py.promotions()` (nouveau) — cherche les programmes actifs de type `promotion`/`buy_x_get_y` avec déclenchement **automatique** (`trigger = 'auto'`, sans code — différent de F15 qui nécessite une saisie), dans leur fenêtre de dates, puis ne garde que les récompenses `reward_type = 'discount'` avec `discount_applicability = 'specific'`. Le champ calculé standard `all_discount_product_ids` (sur `loyalty.reward`) résout déjà la liste des produits concernés qu'ils soient définis un par un ou via catégorie/étiquette/domaine — pas besoin de réimplémenter cette résolution. **Volontairement exclus** : remises "toute la commande"/"produit le moins cher" (pas de sens pour une tuile isolée) et codes promo F15 (nécessitent une saisie, pas un badge passif). **Flutter** : `OdooApiClient.getPromotedIds()`, fusionné dans les 4 écrans qui affichent des tuiles produit (Accueil, Catalogue, Recherche, Favoris) exactement comme le stock (`product['on_promo']`). `ProductGridTile` affiche un badge "Promo" (vert, coin haut-gauche) — masqué si le produit est épuisé (l'indisponibilité prime). **Bug signalé par l'utilisateur (programme "Promotions" correctement configuré — actif, règle avec minimum d'articles, récompense 10% sur produits spécifiques — mais aucun badge côté app ET aucune remise réellement appliquée sur la commande, même après vérification stock/redémarrage backend/relance complète Flutter)** : diagnostiqué en remontant au code source Odoo 19 (`addons/loyalty/models/loyalty_reward.py`) — le champ calculé `all_discount_product_ids` ne se calcule QUE si le paramètre système `loyalty.compute_all_discount_product_ids` vaut `enabled` ; sinon il reste vide en permanence, quelle que soit la config du programme. Non activé par défaut dans Odoo (garde-fou de performance), ce qui explique les deux symptômes à la fois : ni le badge (dépend de ce champ dans notre contrôleur), ni la remise réelle sur la commande (le moteur `sale_loyalty` s'appuie sur le même champ pour résoudre les lignes éligibles à une remise "produits spécifiques"). **Corrigé** en forçant ce paramètre via une nouvelle donnée de module plutôt qu'une manip manuelle dans Settings : `data/loyalty_config.xml`. **2e itération (erreur remontée par l'utilisateur au chargement)** : la 1re version utilisait un `<record model="ir.config_parameter">` en création directe, qui a provoqué une violation de contrainte d'unicité sur `key` (`ir_config_parameter_key_uniq`) — la clé existait déjà en base, créée par défaut par `loyalty`/`sale_loyalty` avec la valeur `disabled`, un `<record>` ne fait pas un "créer ou mettre à jour" sur un champ autre que l'external ID. Remplacé par un appel `<function model="ir.config_parameter" name="set_param" eval="(...)"/>` — `set_param()` est l'API standard Odoo pour ce cas exact (créer si absent, sinon mettre à jour), sans `noupdate` — réappliqué à chaque mise à jour du module, purement technique. **Nécessite `-u echango_order`** (nouvelle donnée). **3e itération, badge visible mais 3 points signalés (capture d'écran)** : (1) badge de la même couleur verte que le bouton "Acheter", peu distinctif — nouvelle couleur dédiée `AppColors.promo` (orange) ; (2) demande d'afficher le pourcentage de remise sur le badge — `promotions()` renvoie désormais un dict `{tmpl_id: pourcentage}` (`reward.discount` si `reward.discount_mode == "percent"`, `None` sinon — le champ s'appelle bien `discount_mode`, pas `discount_type`, vérifié contre le code source ; le pourcentage le plus avantageux est gardé si plusieurs récompenses visent le même produit) au lieu d'une simple liste d'ids ; `OdooApiClient.getPromotedIds()` renommé `getPromotions()` (`Map<int, double?>`), badge affichant `-{percent}%` (nouvelle clé `catalog.onPromoPercent` FR/AR) si connu, sinon le texte générique "Promo" (remise à montant fixe/par point, badgée sans valeur trompeuse) ; (3) **remise jamais répercutée sur le panier ni le récapitulatif de checkout malgré le badge visible** — diagnostiqué en remontant au code source `sale_loyalty` (`sale_order.py`) : `_update_programs_and_rewards()` (qui recalcule/applique les récompenses automatiques) n'est déclenché par Odoo que par `action_confirm()` ou un recalcul de prix, jamais par un simple `create()`/`write()` ORM sur `sale.order.line` — nos routes panier (`cart_controller.py`, contrôleur `sudo()` custom, pas le formulaire JS standard des Ventes) ne le déclenchaient donc jamais avant la confirmation finale. **Corrigé** en appelant `order.sudo()._update_programs_and_rewards()` au début de `_cart_payload()` (donc à chaque consultation/modification du panier — `/echango/cart`, `add`, `update`, `remove`, `reorder` — un seul point plutôt que de le répéter dans chaque route). **4e itération : remise toujours absente signalée par l'utilisateur + demande d'affichage** — `CartScreen` (écran "Mon Panier") n'affichait en réalité **aucune ligne de réduction du tout** (contrairement au récapitulatif checkout qui l'avait déjà) : ajoutée entre sous-total et total, même convention (`checkout.discountLabel`, couleur `AppColors.promo` — désormais utilisée aussi sur le récapitulatif checkout à la place de `AppColors.primary`, pour une couleur cohérente entre badge et montant de réduction partout dans l'app). **Le récapitulatif checkout, lui, a bien le code d'affichage depuis le début** (`if (cart.discount != 0)`) — si la remise n'y apparaît toujours pas non plus après ce correctif, le problème n'est pas côté app mais dans le calcul Odoo lui-même : pistes non encore vérifiées en l'absence d'accès direct à l'instance de test (voir code source `sale_loyalty/models/sale_order.py._update_programs_and_rewards`) — (a) `rule_ids.mode` doit valoir `auto` sur **toutes** les règles du programme, pas seulement `program.trigger` (un champ indépendant, découvert en creusant ce correctif) ; (b) `limit_usage`/`max_usage` du programme : si déjà atteint après plusieurs tests, le programme est silencieusement exclu (`total_order_count >= max_usage`) ; (c) `sale_ok` du programme (disponibilité "Vente", pas seulement POS/eCommerce) ; (d) `pricelist_ids` du programme, si restreint à une liste de prix ne correspondant pas à celle du devis. **5e itération, config validée correcte par l'utilisateur (capture d'écran : Promotions, pas de limite d'utilisation, disponible sur Ventes, produits identiques règle/récompense) — test directement dans Odoo (pas l'app) : la remise ne s'applique PAS non plus en ajoutant simplement le produit dans un devis standard, il faut cliquer sur le bouton "Récompense"** : root cause enfin isolée en lisant le code du bouton (`action_open_reward_wizard`, `sale_loyalty/models/sale_order.py`) — `_update_programs_and_rewards()` seul recalcule l'éligibilité mais n'ajoute pas la ligne de remise ; il faut en plus `_apply_program_reward()` sur la récompense réclamable, exactement ce que fait le bouton. `cart_controller.py._cart_payload()` appelle maintenant `order.sudo().action_open_reward_wizard()` (méthode publique du bouton, directement) au lieu de `_update_programs_and_rewards()` seul — reproduit exactement le comportement validé manuellement par l'utilisateur, y compris la simplification "une seule récompense réclamable = appliquée directement, sinon assistant de choix ignoré" (même logique que F15). **6e itération, écart de prix Odoo ($320) vs App (295 €) signalé par l'utilisateur (captures d'écran)** — deux causes distinctes : (1) Accueil/Catalogue/Recherche affichent `list_price` (prix catalogue brut), le Panier affichait `sale.order.line.price_unit`, calculé par Odoo via la liste de prix (`pricelist_id`) assignée au client — peut légitimement différer de `list_price` selon la liste de prix/devise du client. **Décision utilisateur : préférer le prix brut partout plutôt que de dépendre de la liste de prix.** `cart_controller.py.add()`/`reorder()` forcent désormais explicitement `price_unit = template.list_price` à la création de chaque ligne, garantissant que panier et catalogue affichent toujours exactement le même montant, sans dépendre de la config de liste de prix du client. (2) Symbole "€" en dur dans 9 fichiers Flutter, indépendamment de la devise réellement configurée sur la société Odoo (`res.company.currency_id`, ici USD sur l'instance de test — d'où le "$" vu côté Odoo contre "€" dans l'app, en plus de l'écart de montant). Nouvel endpoint `/echango/currency` (`catalog_controller.py`, `auth="public"` — la vitrine F00 affiche des prix avant connexion) renvoyant `symbol`/`position` (`res.currency`, standard). Nouveau `state/currency_state.dart` (`CurrencyState`, `ChangeNotifier`, rafraîchi une fois au démarrage dans `main.dart`, avant même la connexion) + `utils/currency.dart` (`formatPrice(context, amount)`, raccourci `context.watch<CurrencyState>().format()`) — remplace les 13 occurrences de `'${x.toStringAsFixed(2)} €'` à travers `product_grid_tile.dart`, `product_detail_screen.dart`, `cart_screen.dart`, `checkout_summary_screen.dart`, `order_confirmation_screen.dart`, `order_history_screen.dart`, `substitution_screen.dart`, `order_tracking_screen.dart`. **Aucun changement de schéma** (nouvelle route + nouveaux fichiers Dart seulement) : `docker compose restart odoo` suffit, pas de `-u`. **7e itération, sous-total/total incorrects signalés par l'utilisateur (capture d'écran, prix brut déjà en place mais calcul faux)** — `amount_subtotal` renvoyait `order.amount_untaxed`, qui inclut déjà la ligne de récompense (négative, comme toute ligne de commande) : ce n'est donc pas un vrai "sous-total brut" mais `amount_total` sans les taxes — d'où un sous-total déjà net affiché (265,50 au lieu de 295) et un total identique au sous-total au lieu de sous-total − réduction. **Corrigé** : `amount_subtotal` est maintenant la somme des lignes produit seules (`price_subtotal`, lignes de récompense exclues) — un vrai sous-total brut ; `amount_total` (champ standard, déjà net) reste la source de vérité pour le montant final payé. Sous-total = brut, Total = brut − réduction, conforme à la demande utilisateur |
| `FavoritesScreen` alignée sur l'affichage Accueil/Catalogue (Flutter uniquement) | Terminé (code), non testé en réel | Demande utilisateur : "Mes favoris" doit avoir le même bouton Acheter/sélecteur de quantité que l'Accueil — c'est un écran pensé pour recommander vite les produits habituels, pas juste les consulter. `favorites_screen.dart` réécrit : la liste (`ListView`+`Card`, une tuile dédiée `_FavoriteTile`) devient une `GridView`+`ProductGridTile` identique aux 3 autres écrans (stock récupéré en plus, comme `HomeScreen._fetchProducts`). Le cœur (toujours plein ici) retire des favoris ; plutôt qu'un filtrage local en direct via `FavoritesState` (testé puis écarté : provoquait un flash "aucun favori" le temps que son premier chargement, asynchrone et indépendant du fetch des favoris, se termine), le retrait déclenche un rechargement complet de l'écran (`_toggleFavorite` → `_load()`), plus simple et sans effet de bord. `_FavoriteTile` (Card/ListTile) supprimée, plus utilisée nulle part. Clé `favorites.removed` retirée (devenue inutile, plus de message de confirmation sur la bascule du cœur, cohérent avec les autres écrans qui restent silencieux sur ce genre d'interaction) |
| Bouton "Acheter" + sélecteur de quantité + cœur favoris sur les tuiles produit (Flutter uniquement) | Terminé (code), non testé en réel | **Décision produit** (suite à échange, wireframe fourni par l'utilisateur) : remplace le petit bouton "+" par un vrai bouton "Acheter" pleine largeur qui, une fois cliqué, se transforme en sélecteur de quantité (− qty +) directement sur la tuile — évite l'aller-retour au panier pour ajuster une quantité. Volontairement **pas repris** de l'image de référence : badge promo, étoiles/avis clients, prix au kg — aucune donnée réelle derrière (avis produits explicitement hors périmètre Phase 1), pas de fausses données affichées. **Ajout du cœur favoris** (déjà demandé séparément) directement sur ces mêmes tuiles, pas seulement depuis l'écran dédié `FavoritesScreen`. **`ProductGridTile`** (widget partagé) étendu avec deux couples de paramètres optionnels indépendants : `cartQty`/`onIncrement`/`onDecrement` (bouton Acheter ↔ stepper, utilisé par Accueil/Catalogue/Recherche) et `isFavorite`/`onToggleFavorite` (cœur en superposition sur l'image, mêmes 3 écrans) — `onAdd` (icône "+" simple) reste inchangé pour l'écran d'ajout de favoris, où un cœur serait redondant. **Nouveau** `state/favorites_state.dart` (`FavoritesState`, `ChangeNotifier` sur le modèle de `CartState`) : ensemble des ids favoris, rechargé à chaque visite d'un écran catalogue (comme le stock), mis à jour localement à la bascule (pas de round-trip complet) — accès direct sans devoir dupliquer la logique de fetch dans les 3 écrans. `CartState` complété avec `quantityFor()`/`incrementProduct()`/`decrementProduct()` (ce dernier retire la ligne si la quantité atteint 0) — `incrementProduct()` réutilise `add()` tel quel car `/cart/add` incrémente déjà côté serveur si la ligne existe. Nouveaux utilitaires `utils/toggle_favorite.dart` (même garde `requireAccount` que l'ajout au panier) et `decrementCartProduct()` (ajouté à `utils/add_to_cart.dart`). **Aucun changement backend** : tout réutilise les endpoints déjà existants (F06 panier, favoris de la veille) |
| Liste de produits favoris (Flutter + backend) | Terminé (code) — **warning de dépréciation corrigé après retour utilisateur** | **Décision produit** (suite à échange avec l'utilisateur) : contrairement aux specs qui listent "favoris" en hors périmètre Phase 1, une version simplifiée a été explicitement demandée — liste initialisée automatiquement par les produits déjà commandés (dédupliqué), modifiable ensuite manuellement (ajout/retrait). **Backend** : nouveau modèle `x_product_favorite` (`partner_id`, `product_tmpl_id`, contrainte unique) — aucun équivalent standard sans `website_sale` (non installé), voir CLAUDE.md § Favoris. `checkout_controller.py.confirm()` appelle `_seed_favorites()` après `action_confirm()` : chaque produit de la commande (hors lignes de récompense/réduction F15) est ajouté aux favoris s'il n'y est pas déjà. Nouveau `controllers/favorites_controller.py` (`/echango/favorites`, `/add`, `/remove`), tout en `sudo()` (comme le reste, `product.template` pas accessible en écriture au portail). Accès interne (`base.group_user`) accordé dans `ir.model.access.csv` mais **pas de vue back-office dédiée** — modèle de jointure sans valeur opérationnelle pour le modérateur. **Nécessite `-u echango_order`** (nouveau modèle + accès). **Flutter** : `OdooApiClient.getFavorites/addFavorite/removeFavorite`, nouvel écran `FavoritesScreen` (Profil → Mes favoris) — liste façon `AddressesScreen` (carte par produit, retrait via icône cœur), bouton "Ajouter un produit" ouvrant un écran de recherche dédié (même anti-rebond que F04 `SearchScreen`, réutilise `ProductGridTile` dont le "+" prend ici le sens "ajouter aux favoris"). Nouvelle route `/profile/product/:productId` (dupliquée comme pour les autres branches du `StatefulShellRoute`, pattern déjà établi) pour permettre le tap-through vers la fiche produit depuis l'onglet Profil. Nouvelles clés `favorites.*`/`emptyStates.favorites*`/`screens.Favorites` en FR/AR. **Warning signalé par l'utilisateur au `-u echango_order`** (`Model attribute '_sql_constraints' is no longer supported, please define models.Constraint on the model`) : Odoo 19 a déprécié l'ancienne syntaxe `_sql_constraints = [(...)]` au profit d'un attribut de classe `models.Constraint(sql, message)` (vérifié contre le code source Odoo 19, `odoo/addons/base/models/res_currency.py`, qui utilise déjà la nouvelle syntaxe). `_sql_constraints` remplacé par `_partner_product_uniq = models.Constraint("unique(partner_id, product_tmpl_id)", "Ce produit est déjà dans les favoris.")` |
| Suivi clients — menu "Clients" en back-office | Terminé | Demande utilisateur : voir la liste des clients de l'app et suivre leurs commandes/infos. Nouveau menu **Echango Order → Clients** (`views/res_partner_views.xml`) — liste tous les comptes portail (`user_ids != False`) avec nom/téléphone/ville/statut de vérification en colonnes (`view_partner_list_customers`, réutilisée aussi par "Clients à valider" qui n'en est qu'un sous-ensemble filtré). **Le détail des commandes d'un client n'a nécessité aucun développement** : le bouton statistique "Ventes" sur la fiche contact (ajouté en standard par le module `sale`, déjà dépendance du module) liste déjà toutes ses commandes en un clic — pas de vue/écran custom à construire, conforme à CLAUDE.md § Principe architecture Odoo. Nécessite `-u echango_order` (nouvelle vue/action/menu) |
| Qualité clients — bug "email obligatoire" bloquant la validation (screenshot) | Terminé | Signalé par l'utilisateur : impossible de cliquer "Valider le client" sur un compte créé via l'app (téléphone + PIN uniquement, jamais d'email — F02) car le champ email du formulaire contact standard devient obligatoire dès qu'un compte utilisateur est lié au partenaire (comportement Odoo connu et documenté de longue date, pensé pour le flux d'invitation portail par email — cf. issue historique odoo/odoo#17855) — le clic sur le bouton déclenche une sauvegarde implicite du formulaire, bloquée par ce champ vide-mais-requis. Neutralisé dans `views/res_partner_views.xml` : `<xpath expr="//field[@name='email']" position="attributes"><attribute name="required">0</attribute></xpath>`, technique standard de surcharge de vue. Nécessite `-u echango_order` (changement de vue) |
| Nettoyage UI signalé par l'utilisateur (screenshot) | Terminé | `widgets/screen_placeholder.dart` affichait systématiquement `screens.<screenKey>.subtitle` (texte de repérage interne type "Confirmation — F08", utile pendant la phase "navigation sans données" mais jamais destiné aux utilisateurs finaux) sur les ~17 écrans qui utilisent encore ce widget — supprimé (un seul point de correction, partagé par tous ces écrans ; `screens.*.subtitle` reste dans les traductions, juste plus affiché, pour ne pas casser `translations_completeness_test.dart` qui vérifie sa présence). Bouton "Retour accueil" (`actions.backHome`) retiré de `OrderConfirmationScreen` — c'était le seul endroit où il apparaissait, gardé "Suivre ma commande" comme unique action |
| Firebase Cloud Messaging configuré | Non démarré | |
| Stockage sécurisé PIN (Keychain/Keystore) | Non démarré | Côté Flutter : `flutter_secure_storage` maintenant ajouté (utilisé pour la session, voir ligne ci-dessus) mais **pas encore appliqué au PIN lui-même** côté device (le PIN n'est de toute façon jamais stocké côté app, seul le hash vit côté Odoo — cette ligne concerne un éventuel besoin futur de mémoriser un PIN localement, à clarifier) |
| Build & run réel sur simulateur/device (iOS/Android) | En cours | `flutter analyze` validé côté utilisateur (une passe, ancienne) ; volume important de nouveau code depuis (F02-F08 branchés sur Odoo) jamais repassé en `flutter analyze`/`flutter run` complet. Non testable dans l'environnement Claude Code (pas de SDK Flutter/Android/Xcode) |
| Pagination des écrans de liste (Flutter + backend) | **Terminé — validé en réel (2026-07-19)** | **Demande utilisateur** : ne plus tout charger d'un coup sur les écrans de liste, chargement par page à la demande (bouton), taille de page par défaut 50. Nouveau `utils/pagination.dart` (`kListPageSize`) et `widgets/load_more_button.dart` (`LoadMoreButton`, bouton "Charger plus" + spinner). `kListPageSize` testé à `4` (chargement à la demande vérifié par l'utilisateur), **repassé à `50` après validation**. Appliqué à 5 écrans : Accueil (F03), Catalogue par catégorie (F04), Recherche (F04, réinitialisé à chaque nouvelle recherche), Favoris — liste principale ET écran d'ajout (Profil), Historique commandes (F09, n'avait auparavant **aucune limite du tout**). Pattern uniforme : état `_offset`/`_hasMore`/`_isLoadingMore` + liste `_extra*` accumulée, page suivante ajoutée via `CustomScrollView`+`SliverGrid`+`SliverToBoxAdapter(LoadMoreButton)` (grilles) ou `ListView.separated` avec un item supplémentaire en pied de liste (historique commandes, liste simple). `_hasMore` déduit de `résultats.length == kListPageSize` (pas de `count` serveur séparé, suffisant ici). **Backend** : `favorites_controller.py.list_favorites()` acceptait déjà `search()` sans limite — `offset`/`limit` ajoutés (défaut `limit=None` : comportement **inchangé** pour `FavoritesState.refresh()`, qui a besoin de l'ensemble complet des ids favoris pour tout le catalogue, pas d'une page ; seul l'écran "Mes favoris" passe `limit` explicitement). Filtre `sale_ok` déplacé du `.filtered()` post-requête vers le domaine (`product_tmpl_id.sale_ok = True`) — sinon `offset`/`limit` auraient porté sur un ensemble incluant des favoris devenus non-vendables, faussant `_hasMore`. Accueil/Catalogue/Recherche/Historique n'ont nécessité aucun changement backend (`searchRead()` supportait déjà `offset`, juste jamais utilisé). Nécessite `-u echango_order` (nouveaux paramètres de contrôleur, pas de changement de schéma — un simple restart suffirait presque, mais `-u` ne coûte rien de plus et reste la commande par défaut recommandée). **Suite (signalé par l'utilisateur juste après)** : tirer l'écran vers le bas ne rafraîchissait rien sur Accueil/Catalogue — ajouté `RefreshIndicator` (`_handleRefresh()` relance le chargement de la 1re page, remet `_offset`/`_hasMore`/liste accumulée à zéro comme un rechargement classique) sur `HomeScreen` et `CatalogScreen` (liste des catégories), avec `physics: const AlwaysScrollableScrollPhysics()` explicite sur le `CustomScrollView`/`ListView` pour garantir le geste même quand le contenu ne remplit pas tout l'écran. Non demandé/pas fait sur Catalogue-par-catégorie/Recherche/Favoris (l'utilisateur a mentionné explicitement "l'accueil et catalogue") |

## 1. Fonctionnalités Phase 1 (F00–F17)

Colonne **Écrans** : placeholders créés + navigables (sans données ni logique métier réelle). Colonne **API Odoo** : intégration backend réelle.

| # | Fonctionnalité | Écrans | API Odoo | QA / critères d'acceptation | Notes |
|---|---|---|---|---|---|
| F00 | Vitrine publique | Terminé | Terminé (code), non testé en réel | ☐ | Voir §0 — `x_vitrine_publique` implémenté, endpoint public dédié (`auth='public'`, premier de ce type dans le module) |
| F01 | Onboarding | Terminé | — | ☐ | Aucun appel API. **Corrigé suite à l'audit multi-agents (2026-07-19, passe PO)** : l'écran était un unique `ScreenPlaceholder` statique où "Suivant" et "Passer" faisaient exactement la même chose — pas de carrousel, contrairement à la spec ("3 slides maximum : Commander / Choisir retrait ou livraison / Suivre sa commande", indicateur `● ○ ○`, navigation swipe + bouton). Réécrit avec un vrai `PageView` (3 slides, icônes Material en illustration faute d'asset graphique dédié), indicateur de page animé, "Suivant" avance slide par slide puis termine sur le dernier, "Passer" fonctionnel dès le premier slide. RTL : `PageView` respecte nativement la `Directionality` ambiante (sens de swipe inversé en arabe), rien à gérer manuellement |
| F02 | Authentification (inscription/connexion/invité) | Terminé | Terminé — inscription + connexion validées en réel via l'app | ☐ | Backend Odoo `/echango/auth/register` + `/login` + client Flutter, testés avec succès de bout en bout (app réelle sur émulateur). Reste : scénario de blocage PIN (5 échecs) pas testé en réel, session non persistée (voir §0), et QA complet (RTL, perf) pas encore passé en revue formelle. Mode invité géré localement (pas d'appel Odoo). **Nom + langue corrigés suite à l'audit multi-agents (2026-07-19)** : `register_step1_screen.dart` ne collectait que le téléphone — `api.register()` acceptait déjà `name`/`lang` côté client depuis longtemps, mais aucun écran ne les renseignait, donc tout compte créé via l'app avait pour nom son numéro de téléphone et aucune langue enregistrée sur `res.partner.lang`. Ajout d'un champ "Nom et prénom" (obligatoire) et d'un sélecteur de langue (FR/AR, même pattern que `LanguageSettingsScreen`) à l'étape 1, propagés via `extra` (désormais un `Map` plutôt qu'une simple chaîne) jusqu'à l'appel `register()` à l'étape 3. Mapping vers les codes `res.lang` Odoo (`fr` → `fr_FR`, `ar` → `ar_001`) — retombe sur `lang=False` côté serveur si le code n'est pas installé (déjà géré, sans effet de bord). La case CGU non bloquante (écart documenté séparément dans le code, `RegisterStep3Screen`) n'a pas été retouchée dans cette passe. **"PIN oublié" corrigé suite à un audit multi-agents (PO/Technique/Sécurité, 2026-07-19)** : l'écran était un cul-de-sac total (`showComingSoon()` quoi que fasse l'utilisateur, aucun SMS, aucune réinitialisation possible). Remplacé par un vrai flux : `/echango/auth/request_pin_reset` (public, rate limité 5/60min) crée une activité pour un modérateur (même mécanisme que la validation de compte) qui recontacte le client par téléphone et réinitialise son PIN via un nouvel assistant Odoo (`x_pin_reset_wizard`, bouton sur la fiche utilisateur). Réponse serveur toujours générique (que le numéro existe ou non) pour ne pas devenir un oracle d'énumération de comptes. `ForgotPinScreen` simplifié : plus de faux champs "code SMS"/"nouveau PIN" qui ne validaient jamais rien, juste le téléphone + confirmation honnête ("notre équipe vous recontactera"). `utils/coming_soon.dart` supprimé (devenu totalement mort, plus aucun appelant). **Nécessite `-u echango_order`** (nouveau modèle assistant + vue) |
| F03 | Accueil | Terminé | **Terminé — validé en réel** (grille produits affichée dans l'app après `-u echango_order`) | ☐ | Voir §0 pour le détail (accès portail accordé, filtre `sale_ok`, pas encore de "produits mis en avant"/bannière) |
| F04 | Catalogue & Recherche | Terminé | Terminé (code) — corrigé après retours utilisateur (catégories + warning `read_group`), non re-testé depuis | ☐ | `CatalogScreen`/`CategoryProductsScreen`/`SearchScreen` (voir §0). Bouton "+" branché sur F06 (plus `showComingSoon`). Restriction `sale_ok` imposée côté serveur par l'`ir.rule` de F03 |
| F05 | Fiche Produit | Terminé | Terminé (code), non testé en réel | ☐ | Voir §0 — `read` standard par id, description Html nettoyée côté client, `qty_available` volontairement absent (bouton "Ajouter au panier" toujours actif, critère QA "épuisé → désactivé" différé) |
| F06 | Panier | Terminé | Terminé (code), non testé en réel | ☐ | Voir §0 — panier = devis Odoo (`sale.order` brouillon) réel, contrôleur custom (portail en lecture seule sur ces modèles par design Odoo). Pas de panier invité (partner temporaire non implémenté, cf. §4) |
| F07 | Checkout & Mode de Réception | Terminé | Terminé (code), non testé en réel | ☐ | Voir §0. GPS n'autoremplit pas l'adresse (décision service de géocodage non prise), pas de notion de "créneau complet" |
| F08 | Confirmation & Suivi Commande | Terminé | Confirmation : terminé (code). Suivi temps réel : non démarré | ☐ | Écran de confirmation avec données réelles (voir §0). Suivi (statuts synchronisés + notifs push) reste un placeholder — dépend de F11 |
| F09 | Historique Commandes & Reorder | Terminé | Terminé (code), non testé en réel | ☐ | Voir §0. Bouton reorder affiché sur toute commande confirmée (simplification — pas de distinction "Livrée" tant que F08/stock.picking n'est pas synchronisé) |
| F10 | Profil Utilisateur | Terminé | Terminé (code), non testé en réel | ☐ | Voir §0 — en-tête/nom/PIN, adresses (CRUD + favorite + GPS optionnel par adresse, "Ma Localisation" fusionnée ici suite retour utilisateur), suppression de compte réelle (PIN + `active=False`), notifications (état permission système). SMS de confirmation après suppression hors scope (pas de fournisseur SMS) |
| F11 | Notifications Push | Non démarré | ☐ | ☐ | Webhook Odoo → FCM. La permission notification (F14) est déjà demandée après confirmation de commande, mais aucun envoi réel |
| F12 | Partage Produit (Deep Link) | Terminé (partiel) | ☐ | ☐ | Bouton partage sur fiche produit (`share_plus`, sheet natif) avec lien placeholder. **Réception du deep link (Universal/App Links) non câblée** — nécessite un domaine réel + choix de techno (Branch.io vs Firebase Dynamic Links, cf. §4) + fichiers natifs `android/`/`ios/` |
| F13 | Pages Légales | Terminé | — (aucun appel API, contenu statique) | ☐ | Voir §0 — texte provisoire rédigé (CGU/confidentialité/mentions légales/cookies), accessible depuis l'inscription ET le profil. **Validation par un juriste requise avant soumission aux stores** |
| F14 | Permissions & États Système | Terminé | ☐ | ☐ | Écran maintenance + permissions réelles (`permission_handler`) : dialog d'explication puis demande système pour GPS (Register/CheckoutAddress/Adresses) et notifications (auto après confirmation commande). **Déclarations natives Android/iOS pas encore ajoutées** (voir CLAUDE.md) |
| F15 | Code Promo | Terminé | Terminé (code), non testé en réel | ☐ | Voir §0 — module standard `sale_loyalty` (Community, LGPL-3), aucun champ custom. **Nécessite la création d'un programme "Discount Code" en back-office avant de pouvoir tester** (voir §0 pour la procédure) |
| F16 | Annulation Commande | Terminé | Terminé (code), non testé en réel | ☐ | Voir §0 — annulation possible tant que `state == 'sale'` (pas de fenêtre de délai distincte, faute de suivi `stock.picking`). Notification push après annulation hors scope (F11) |
| F17 | Substitution Produit | Terminé | Terminé (code), non testé en réel | ☐ | Voir §0 — signalement manuel back-office (`x_substitution_produit`), accepter/refuser réels. Bouton visible dans le suivi seulement si une substitution est en attente. Notification push + auto-substitution après 30 min hors scope (dépend de F11) |

## 2. Sécurité (transversal — bloquant pour release)

| Exigence | Statut |
|---|---|
| HTTPS/TLS 1.3 sur tous les appels | Non démarré |
| PIN jamais stocké en clair (Keychain/Keystore) | Terminé | Le PIN n'est jamais persisté côté app (ni en clair ni haché) — seul le hash vit côté Odoo (`res.users.x_pin`, voir F02). Rien à stocker côté Keychain/Keystore pour le PIN lui-même |
| Session 24h + re-auth PIN | Terminé (code), non testé en réel | Politique d'expiration par inactivité explicite et testable implémentée de bout en bout, sans dépendre de la configuration interne (non vérifiable ici, pas de Docker) du mécanisme de session générique d'Odoo. **Backend** : `res.users.x_last_activity` (nouveau champ Datetime, `models/res_users.py`) mis à jour à la connexion (`auth_controller.login`) et à chaque appel réussi via le nouveau décorateur `require_fresh_session` (`controllers/session_utils.py`), appliqué aux 25 endpoints `auth="user"` des contrôleurs custom (cart/checkout/profile/favorites/order/catalog) — renvoie `{"error": "auth.session_expired"}` si plus de 24h se sont écoulées. **Flutter** : `AuthState` gère un nouveau statut `sessionExpired` (distinct d'`unauthenticated`) qui conserve le téléphone connu (`loginAsUser(phone:)` le persiste désormais) — `expireSession()` (au lieu d'un `logout()` complet) bascule dessus, déclenché soit par le rejet serveur (`onSessionExpired`, y compris `auth.session_expired` renvoyé par `require_fresh_session` — ajouté au mapping `_errorCodeMap`), soit par `AuthState.checkInactivity()` (comparaison locale à `sessionInactivityLimit` = 24h, appelée au lancement de l'app et à chaque retour au premier plan via `WidgetsBindingObserver`, `OdooApiClient.onActivity` réarmant l'horodatage à chaque appel réussi). Nouvel écran `ReauthPinScreen` (`/reauth`, imposé par le `redirect` de go_router tant que `isSessionExpired`) : redemande uniquement le PIN (téléphone déjà connu), avec option "Se déconnecter" en repli. Nouvelles clés `screens.Reauth.{title,subtitle}` en FR/AR (le reste réutilise des clés existantes : `errors.auth.session_expired`, `actions.logIn/logout`, `auth.phoneLabel/pinLabel`). **Nécessite `-u echango_order`** (nouveau champ `x_last_activity`). **Trou trouvé et comblé suite à l'audit sécurité multi-agents (2026-07-19)** : la politique ne couvrait que les 25 endpoints `/echango/*` — l'historique de commandes (`OrderHistoryScreen`) et le suivi (`OrderTrackingScreen`, `SubstitutionScreen`) passaient par des `search_read` directs sur `sale.order`/`sale.order.line` via `/web/dataset/call_kw`, un chemin qui échappe entièrement à `require_fresh_session`. Un cookie de session volé restait donc exploitable pour lire ces données personnelles indéfiniment, bien au-delà de 24h. Plutôt que de risquer une surcharge de `ir.http._authenticate`/`_dispatch` (point d'extension standard mais dont la signature exacte en Odoo 19 n'a pas pu être vérifiée dans ce sandbox — un mauvais paramètre y casserait TOUTE requête, pas seulement celle-ci), migration de ces 3 lectures vers de nouveaux endpoints custom déjà couverts par le décorateur existant : `/echango/order/list` (remplace le `search_read` paginé de l'historique), `/echango/order/detail` (remplace les deux `search_read` du suivi, commande + lignes), et `get_substitution`/`/echango/order/substitution` accepte désormais `order_ref` en plus de `order_id` (évite à `SubstitutionScreen` un `search_read` séparé juste pour résoudre l'id). `_owned_order()` (`order_controller.py`) généralisé pour accepter l'un ou l'autre. Le catalogue (produits, stock, promotions) n'est pas concerné — seules les données personnelles (commandes) étaient en cause. **Logout incomplet corrigé (même audit)** : le bouton "Se déconnecter" de `ReauthPinScreen` (et le repli quand aucun téléphone n'est connu) n'appelait que `AuthState.logout()`, sans `OdooApiClient.clearSession()` — le cookie de session restait valide dans `flutter_secure_storage` et exploitable directement contre l'API, contournant l'app. Nouvelle fonction centralisée `utils/logout.dart` (`fullLogout(context)`, efface le cookie ET l'état local) — appliquée à `ReauthPinScreen` et `ProfileScreen` (qui faisait déjà les deux appels séparément, maintenant via la même fonction). `delete_account_dialog.dart` déjà correct laissé inchangé (résout `OdooApiClient`/`AuthState` une fois en haut de la fonction pour rester valide après l'appel réseau, pattern différent par nécessité) |
| Délai progressif tentatives PIN (1s/2s/4s/8s + blocage) | En cours — implémenté serveur (`res.users._check_pin`), non testé en réel. **Race condition corrigée suite à l'audit sécurité du 2026-07-19** : `_check_pin` lisait `x_pin_fail_count`/`x_pin_locked_until` puis les réécrivait sans verrou — des tentatives de PIN envoyées en parallèle (au lieu de séquentiellement) pouvaient toutes lire le même compteur avant qu'aucune n'ait écrit sa mise à jour ("lost update"), affaiblissant le blocage progressif. `SELECT ... FOR UPDATE` (verrou explicite sur la ligne, lecture fraîche en SQL direct plutôt que le cache ORM) ajouté avant la vérification |
| Filtrage strict des champs API (pas de surexposition) | Audit formel réalisé | **Revue de tous les contrôleurs `/echango/*`** (cart/checkout/profile/favorites/order/catalog/vitrine/auth) : chaque endpoint qui prend un ID en paramètre vérifie explicitement la propriété avant d'agir (`_owned_line`/`_owned_order`/`_owned_address`, domaine `partner_id = request.env.user.partner_id` systématique) — aucun cas trouvé où un ID pourrait être utilisé pour lire/modifier les données d'un autre client (IDOR). Les endpoints publics (`auth="public"`) ne renvoient que des données non sensibles filtrées par domaine (`sale_ok`/`x_vitrine_publique`). **Un vrai problème de surexposition de champs trouvé et corrigé** : `product.template`/`product.product` sont accordés en lecture au groupe portail (`access_product_template_portal`, restreint aux produits vendables par `ir_rule.xml`) pour permettre les `search_read` directs du catalogue (Accueil/Catalogue/Recherche) via `/web/dataset/call_kw` — mais `ir.model.access`/`ir.rule` filtrent les **lignes**, pas les **champs** : un appel direct à cet endpoint (hors app, avec une session portail valide) pouvait demander n'importe quel champ du modèle, y compris `standard_price` (prix de revient), qu'un client ne devrait jamais pouvoir consulter. Les restrictions vues dans le formulaire back-office (`groups` sur les *vues*) n'ont aucun effet sur l'ORM/API — seule une restriction `groups` sur le *champ* lui-même protège vraiment. Corrigé par une surcharge minimale (`models/product_template.py`, `standard_price = fields.Float(groups="base.group_user")` sur `product.template` ET `product.product`, seul l'attribut `groups` est modifié — le reste de la définition d'origine, `compute`/`company_dependent`/etc., est préservé par le mécanisme standard de fusion des attributs de champ hérités). **À vérifier par l'utilisateur après `-u echango_order`** : le prix de revient continue de s'afficher/se calculer normalement en back-office (surcharge d'un champ standard existant, jamais testée en réel faute de Docker dans le sandbox — risque de régression plus élevé qu'un champ neuf). **Point non tranché, laissé en observation** : les lectures directes `sale.order`/`sale.order.line` (historique, suivi commande) reposent sur la règle portail standard d'Odoo restreignant `sale.order` au partenaire propriétaire (fonctionnalité "Mes commandes" du portail client, cœur du module `sale` — confirmée indirectement par les tests réels de l'utilisateur, aucune commande d'un autre client jamais vue) ; l'existence d'une règle équivalente au niveau ligne (`sale.order.line`) n'a pas pu être vérifiée contre le code source (accès réseau au dépôt Odoo bloqué dans ce sandbox) — risque faible (donnée peu sensible : nom de produit + quantité, `id` de commande jamais exposé dans l'app, à deviner) mais à garder en tête si une donnée plus sensible est un jour ajoutée à `sale.order.line` |
| Rate limiting endpoints publics (vitrine, deep links) | Terminé (code), corrigé après test réel | Nouveau modèle `x_rate_limit` (`models/rate_limit.py`) : compteur à fenêtre fixe par clé (`action:ip`), incrémenté/réinitialisé par `_hit(key, limit, window_minutes)`, purgé quotidiennement par un `ir.cron` standard (`data/rate_limit_data.xml`, `model._gc()`) — pas de mécanisme de nettoyage maison. Décorateur `rate_limited(action, limit, window_minutes)` (`controllers/rate_limit.py`), appliqué aux 4 endpoints `auth="public"` : `/echango/auth/login` (10/5min, le plus sensible — brute-force par énumération de téléphones, distinct du blocage PIN par compte déjà existant), `/echango/auth/register` (5/60min, anti spam de comptes), `/echango/currency` (60/1min), `/echango/vitrine/products` (120/1min). Renvoie `{"error": "rate_limited"}` au-delà de la limite, mappé côté Flutter vers `AppError.serverRateLimited` (`errors.server.rate_limited`, nouvelles clés FR/AR). Ne remplace pas un WAF/reverse proxy en production (pas encore déployé, voir ligne HTTPS/TLS ci-dessus) — défense en profondeur applicative en attendant. **Bug trouvé au premier démarrage réel** : `ir_cron_rate_limit_gc` utilisait `numbercall` — champ supprimé du modèle `ir.cron` en Odoo 19 (les crons récurrents standards n'ont plus de compteur d'occurrences, seulement `active`), faisait planter tout le chargement du registre au démarrage (`ValueError: Invalid field 'numbercall' in 'ir.cron'`). Champ retiré de `data/rate_limit_data.xml`. **Nécessite `-u echango_order`** (nouveau modèle + cron). **Race condition corrigée suite à l'audit sécurité du 2026-07-19** : `_hit()` faisait un `search()` puis un `write()`/`create()` sans verrou ni contrainte d'unicité — deux requêtes concurrentes sur la même clé pouvaient créer deux lignes dupliquées (capacité de contournement) ou perdre un incrément ("lost update"), permettant de dépasser la limite en envoyant des appels en rafale. Ajout d'une contrainte unique sur `key` + `SELECT ... FOR UPDATE` (verrou explicite, lecture fraîche en SQL direct) sur le chemin de mise à jour, et d'un repli `cr.savepoint()` sur le chemin de création (capture la violation de contrainte si une autre requête vient de créer la même clé entre-temps, puis bascule sur une mise à jour au lieu de laisser l'erreur remonter). **Rate limiting par numéro ajouté (même audit)** : la limite `auth.login` (10/5min) est indexée par IP — contournable en distribuant les tentatives sur plusieurs IP (proxy/VPN) pour cibler un seul numéro de téléphone. Ajout d'une limite supplémentaire indexée par numéro (`auth.login.phone:{phone}`, 10/15min, appel direct à `_hit()` dans `auth_controller.login` plutôt que le décorateur, keyé différemment). **Non résolu, accepté comme limite connue** : les codes d'erreur distincts `auth.phone_already_registered` (register) et `auth.account_locked`/`auth.invalid_credentials` (login, un numéro inexistant ne peut jamais atteindre `account_locked`) permettent de déterminer si un numéro de téléphone est inscrit — corriger ça dégraderait l'UX (messages d'erreur moins clairs) sans bloquer un attaquant déterminé (le rate limiting ralentit, ne bloque pas). Laissé tel quel, documenté pour référence future |

## 3. i18n / RTL (transversal)

| Exigence | Statut |
|---|---|
| Externalisation complète des chaînes (fichiers de traduction FR/AR) | Terminé — vérifié automatiquement (`translations_completeness_test.dart`) |
| Layout RTL validé sur chaque écran | En cours | Audit statique du code effectué (recherche des patterns RTL-unsafe : `EdgeInsets.only(left/right:)`, `TextAlign.left/right`, `Alignment.centerLeft/Right`, icônes directionnelles). Codebase globalement RTL-safe (`Row`/`Column`/`EdgeInsets.symmetric` partout). Un vrai bug trouvé et corrigé : `Icons.chevron_right` sur les lignes de catégorie (`catalog_screen.dart`) ne se retournait pas en RTL → bascule explicite vers `Icons.chevron_left` selon `Directionality.of(context)` (deux essais invalides trouvés par `flutter analyze` côté utilisateur avant la version correcte : `matchTextDirection: true`, paramètre inexistant sur `Icon` ; puis `TextDirection.rtl` non qualifié, en conflit avec un symbole `TextDirection` distinct exporté par `easy_localization` dans ce fichier — résolu avec un import préfixé `import 'dart:ui' as ui;` et `ui.TextDirection.rtl`). Un point non tranché, laissé tel quel volontairement : badge Promo/Épuisé et cœur favori sur les tuiles produit (`product_grid_tile.dart`) sont positionnés en coins physiques (`Positioned`/`Alignment.centerRight`, pas `PositionedDirectional`/`AlignmentDirectional`) — reste dans les mêmes coins visuels quelle que soit la langue ; possiblement voulu (pattern courant en e-commerce), à confirmer visuellement. **La validation visuelle réelle écran par écran (débordements, wrapping, rendu police arabe) reste à faire côté utilisateur** — non exécutable dans le sandbox Claude Code (pas d'émulateur) |
| Formats date/heure localisés | Non démarré |

## 4. Points de vigilance ouverts (décisions à prendre — voir specs §6 / §7)

- [x] ~~Race condition sur la pagination "Charger plus" (5 écrans)~~ — trouvée à l'audit technique du 2026-07-19 : `home_screen.dart`, `catalog/search_screen.dart` (+ `favorites_screen.dart._FavoritesAddScreenState`, même écran de recherche pour ajouter un favori), `catalog/category_products_screen.dart`, `favorites_screen.dart` (liste principale), `order/order_history_screen.dart` partageaient le même défaut : si un rechargement complet (nouvelle recherche, tirer-pour-rafraîchir, retry) démarrait pendant qu'un `_loadMore()` était encore en vol, celui-ci écrivait son résultat (devenu obsolète) dans la liste fraîchement vidée pour la nouvelle page 1 — cas explicitement reproductible en tapant une nouvelle recherche juste après avoir appuyé sur "Charger plus". Corrigé par un jeton de génération (`int _loadGeneration`, incrémenté à chaque rechargement complet) vérifié avant d'appliquer le résultat d'un `_loadMore()` — un résultat dont la génération ne correspond plus à la génération courante est silencieusement ignoré plutôt qu'appliqué. `_offset`/`_hasMore` (mutés comme effet de bord à l'intérieur de la fonction de fetch partagée) peuvent encore être écrasés un court instant par un appel obsolète avant que la vérification n'intervienne — limite résiduelle acceptée (auto-corrigée au prochain chargement, pas de donnée persistée en jeu), documentée plutôt que de refactorer les 5 écrans pour l'éliminer entièrement
- [x] ~~`change_pin_screen.dart` : `_isSubmitting` muté sans `setState()`~~ — trouvé à l'audit technique du 2026-07-19 : contrairement à tous les autres écrans du même type (`RegisterStep3`, `LoginScreen`, `ReauthPinScreen`...), la mutation directe (`_isSubmitting = true/false`) ne déclenchait aucun rebuild — le garde-fou anti double-soumission fonctionnait quand même (simple champ Dart, pas besoin de rebuild pour ça), mais aucun état "en cours" n'était exploitable par l'UI. Passé à `setState()`, cohérent avec le reste de l'app
- [x] ~~Texte non traduit "Version : " dans `about_screen.dart`~~ — trouvé à l'audit technique du 2026-07-19 : le préfixe "Version : " était en dur en français, en violation de la règle CLAUDE.md § Traductions (préfixes de labels inclus). Nouvelle clé `about.versionLabel` (`"Version : {version}"` / équivalent AR) avec `namedArgs`, même pattern que `catalog.onPromoPercent`
- [x] ~~Fuites de `TextEditingController` (3 écrans)~~ — trouvé à l'audit technique du 2026-07-19, corrigé : `delete_account_dialog.dart` (`pinController`, fonction top-level donc pas de `State.dispose()` — nettoyé via `.whenComplete(pinController.dispose)` sur le `Future` de `showDialog`), `profile_screen.dart._editName` (même pattern, `.whenComplete(controller.dispose)`), et `reauth_pin_screen.dart` où un `TextEditingController` était recréé à **chaque `build()`** pour afficher le téléphone en lecture seule (jamais disposé, fuite qui s'accumulait à chaque tentative de PIN échouée puisque `setState` redéclenche `build()`) — remonté en champ `late final` de la `State`, initialisé une fois, disposé dans `dispose()`
- [x] ~~Invité éjecté silencieusement depuis "Mes adresses"/"Mes favoris"/"Modifier PIN"/"Supprimer mon compte"~~ — bug trouvé à l'audit multi-agents du 2026-07-19 (passe PO), corrigé : `AddressesScreen`/`FavoritesScreen`/`ChangePinScreen`/`showDeleteAccountDialog` appellent tous des endpoints `auth="user"` sans session Odoo réelle pour un invité (`SessionStatus.guest`, traité comme "authentifié" par le routeur pour l'accès aux onglets principaux) — l'erreur de session remontait jusqu'à `AuthState.expireSession()`/`logout()`, éjectant silencieusement l'invité vers la vitrine en pleine navigation dans son propre profil. `ProfileScreen` applique désormais la même garde `requireAccount()` que le panier (F06) avant ces 4 actions — propose l'inscription au lieu de planter silencieusement, aucun changement pour un compte déjà connecté (`requireAccount` renvoie `true` immédiatement dans ce cas). `OrderHistoryScreen`/`LanguageSettingsScreen`, eux, géraient déjà correctement le cas invité (vérifié, pas de changement nécessaire)
- [ ] Valider les statuts de commande Odoo avec l'Expert Odoo
- [x] ~~Définir le délai d'annulation commande autorisé~~ — fait avec une simplification par défaut (F16, voir §0) : annulation possible tant que `state == 'sale'` ("Confirmée"), pas de fenêtre de délai distincte faute de suivi `stock.picking` synchronisé. À revoir si un vrai suivi préparation est ajouté un jour
- [ ] Définir le délai de conservation des données après suppression de compte (RGPD)
- [ ] Définir les zones de livraison et codes postaux couverts
- [ ] Valider contenu CGU / politique de confidentialité avec un juriste — texte provisoire rédigé (F13, voir §0), à ne pas publier sur les stores sans validation juridique
- [ ] Choisir la techno deep link (Branch.io vs Firebase Dynamic Links)
- [ ] Définir la page desktop de destination pour les deep links
- [ ] Confirmer la disponibilité des endpoints Odoo 19 listés dans les specs
- [x] ~~Valider le module coupon/promo compatible Odoo 19~~ — fait (F15, voir §0) : `sale_loyalty`/`loyalty` (Community, LGPL-3), confirmé compatible Odoo 19 contre le code source. Programme "Discount Code" à créer manuellement en back-office avant de pouvoir tester en réel
- [ ] Valider la durée de blocage du compte après 5 échecs de PIN (15 min implémenté par défaut, arbitraire — voir §0 module F02)
- [x] ~~Concevoir un signal de disponibilité stock exposable au portail sans ouvrir tout le module stock~~ — fait (`/echango/catalog/stock`, contrôleur en `sudo()`, voir §0), badge "Épuisé" et blocage "Ajouter au panier" actifs. Non testé en réel
- [ ] Panier invité : specs §"Mode invité" prévoit une "commande liée à un partner temporaire Odoo", pas implémenté (F06 ne gère que les comptes connectés, cf. §0) — décider comment créer/retrouver ce partner temporaire sans session Odoo authentifiée avant de s'y attaquer
- [ ] Choisir un service de géocodage inverse (Google Maps / Mapbox / Nominatim) pour le bouton "Utiliser ma position GPS" en F07 — actuellement demande juste la permission (F14) sans remplir l'adresse
- [x] ~~Gestion de la capacité des créneaux ("créneau complet grisé", QA F07)~~ — fait : nouveau modèle `x_timeslot_capacity` (`reception_mode`, `hour` heure locale 0-23, `max_orders`), géré en back-office (menu Echango Order > Capacité des créneaux). Les créneaux restent générés côté client (`utils/timeslots.dart`, seule source de vérité) — pas de capacité configurée pour une heure donnée = jamais complet (comportement par défaut inchangé, rien à configurer pour que l'app continue de fonctionner comme avant). Nouvel endpoint `/echango/checkout/timeslots` (`checkout_controller.py`) : reçoit les créneaux candidats (`{start, hour}`), renvoie ceux complets (nombre de `sale.order` non annulées déjà sur ce créneau exact >= capacité) — `CheckoutTimeslotScreen` grise ces créneaux (`ListTile` non interactif + libellé "Complet", `errors.checkout.slot_full` réutilisé) au lieu du `RadioListTile` sélectionnable, échec de chargement traité en silence volontaire (ne bloque jamais la sélection, juste pas de grisage). **Vérification dupliquée côté serveur dans `confirm()`** (`_slot_is_full`, partagé avec `timeslots()`) — un appel direct à l'endpoint en contournant l'UI est aussi bloqué (`{"error": "checkout.slot_full"}`, déjà mappé côté Flutter via la constante `AppError.checkoutSlotFull` qui existait mais n'était pas encore câblée). **Nécessite `-u echango_order`** (nouveau modèle + vue + menu)
- [ ] Définir au moins une `x_delivery_zone` réelle en back-office (Réglages → Echango Order → Zones de livraison) avant de pouvoir tester un parcours livraison de bout en bout — sinon toute adresse ressort "hors zone"
- [x] ~~Gestion du fuseau horaire pour `x_creneau`~~ — fait : `formatOdooDatetime` convertit désormais explicitement en UTC (`dt.toUtc()`) avant d'envoyer un `Datetime` à Odoo, et le nouveau `parseOdooDatetime` (symétrique) traite toute chaîne reçue d'Odoo comme UTC puis la reconvertit en heure locale de l'appareil — `DateTime.tryParse` seul traite une chaîne sans fuseau comme déjà locale, d'où le bug initial. 3 points de lecture mis à jour : confirmation de commande, suivi de commande (`x_creneau`), historique (`date_order`)
- [x] ~~Relier les adresses sauvegardées (F10) au checkout (F07)~~ — fait : `CheckoutAddressScreen` propose désormais la liste des adresses sauvegardées (favorite pré-sélectionnée) avec une option "nouvelle adresse" qui retombe sur le formulaire libre existant. `checkout_controller.py.confirm()` accepte un nouveau paramètre `address_id` : s'il est fourni, réutilise directement le contact `delivery` existant comme `partner_shipping_id` (zone de livraison revérifiée côté serveur sur `shipping.city`/`shipping.zip`) au lieu d'en créer un nouveau à chaque commande ; le chemin `street`/`city`/`zip_code`/`notes` (adresse ponctuelle non sauvegardée) reste inchangé en repli. `CheckoutState`/`OdooApiClient.confirmOrder()` transmettent `addressId` de bout en bout. Un échec de chargement des adresses sauvegardées n'empêche pas de continuer (repli automatique sur le formulaire libre, erreur signalée via `AppMessenger`, jamais silencieuse). **Libellé corrigé suite à l'audit multi-agents (2026-07-19, passe PO)** : l'option "nouvelle adresse" réutilisait le même texte (`addresses.addTitle`, "Ajouter une adresse") que le vrai bouton d'ajout permanent dans `AddressesScreen`, laissant croire qu'elle créait une adresse sauvegardée — nouvelle clé dédiée `checkout.newAddressOption` ("Nouvelle adresse pour cette commande"), sans ambiguïté sur le fait qu'elle ne sera pas réutilisable au prochain checkout
- [x] ~~Vérifier/renforcer qu'une session Odoo déjà ouverte est bien invalidée immédiatement après désactivation du compte~~ — durci : `delete_account` écrit désormais aussi un `password` aléatoire (`secrets.token_urlsafe(32)`) en plus de `active=False`. `password` fait partie des champs de sécurité standards dont dépend le calcul du token de session côté Odoo (mécanisme documenté : changer le mot de passe invalide les autres sessions), donc toute session déjà ouverte sur un autre appareil expire immédiatement à la requête suivante, sans dépendre du comportement (non vérifiable ici, pas de Docker) d'`active` seul sur le token de session. L'app n'utilise jamais l'authentification par mot de passe standard (PIN custom uniquement), donc aucun effet de bord. **Reste à confirmer en réel** (comportement HTTP effectif après déconnexion forcée) côté utilisateur
- [ ] Choisir un fournisseur SMS pour la confirmation après suppression de compte (specs QA F10) — hors scope tant que non décidé
- [x] ~~Qualité clients : notifier automatiquement le modérateur quand un nouveau compte attend validation~~ — fait avec le mécanisme standard Odoo : `res.partner._notify_verification_pending()` (`models/res_partner.py`) appelle `activity_schedule()` (mixin `mail.activity.mixin`, déjà hérité nativement par `res.partner`) pour créer une activité "à faire" — visible dans le menu Activités et le chatter de la fiche client — appelée depuis `/echango/auth/register` juste après la création du partenaire en `pending`. Assignée au premier utilisateur `base.group_system` trouvé (même groupe que celui restreignant déjà `x_pin`), repli sur OdooBot (id=1) si aucun admin dédié. Pas d'email (aucun serveur SMTP configuré dans ce projet) — une activité back-office suffit. Ajout de `mail` aux dépendances du module (`__manifest__.py`, transitivement déjà présent via `sale` mais rendu explicite) — **nécessite `-u echango_order`**

## 5. Roadmap macro (rappel — voir `docs/specs_macro_drive_transport.md` §8)

- **Semaines 1-2** : développement app client (catalogue, panier, suivi statuts, bilingue FR/AR)
- **Semaine 3** : tests fonctionnels, validation RTL arabe, corrections
- **Semaine 4** : soumission App Store + Google Play

---

*Mettre à jour ce fichier à chaque avancée significative (changement de statut d'une fonctionnalité, décision prise sur un point de vigilance, etc.).*
